# Governor Oracle Draw Source

**Governor:** Gary Teh

**Canonical source for all oracle draws (until stated otherwise):**

https://truesight.me/programs/truesight-grounding/credentials/#pk-iWL9OH9hpE_D

This is the lineage-credentials practice history viewer for Gary's `truesight-grounding` program. All daily oracle draws (`[PRACTICE EVENT]` of type `oracle-consultation`) are recorded here under:

`lineage-credentials/programs/truesight-grounding/pk-iWL9OH9hpE_D/practice/`

**Do NOT rely on `oracle_logs` repo for Gary's draws** — that repo is stale and not the authoritative source. The lineage-credentials repo is the system of record.

## How to read today's draw

1. List the practice files in:
   `lineage-credentials/programs/truesight-grounding/pk-iWL9OH9hpE_D/practice/`
2. Find the file matching today's date (YYYY-MM-DD pattern in the filename timestamp)
3. Read the JSON payload for hexagrams, changing lines, advisory summary

## Fallback

If the lineage-credentials path is empty for a given day, the draw may not have been recorded yet. Ask the governor if they've done their morning grounding.
