# Handoff Manifest — active execution handoffs (single source of truth)

Machine-readable index of all active execution handoffs. Most rows hand off to **Sophia**
(the autopilot), who updates them when she starts, resumes, or completes one. Rows may also
target another runner — e.g. the **`nelanco-claude`** interactive box — in which case the
row says so explicitly in its title. Whoever executes updates the row.

**This file is the single source of truth for handoff state**, including Telegram
topic/thread linkage (consolidated 2026-07-18 — see
`plans/HANDOFF_REGISTRY_CONSOLIDATION_PLAN.md`). `sophia/SOPHIA_HANDOFFS.md` no longer
carries a duplicate registry table; it holds only the Sophia-specific trigger protocol (GO
convention, trigger template, thread-management rules).

**Protocol for all LLMs:**
1. Before acting on any handoff, `git pull` agentic_ai_context remote `main` to get the latest plans.
2. Check this manifest — it is the only table you need for status, resume state, and Telegram linkage.
3. For the Sophia trigger protocol (how to ping her, the GO convention, thread rules), see `sophia/SOPHIA_HANDOFFS.md`.
4. Optional: run `python3 scripts/validate_handoff_manifest.py` to check this file's structural consistency (schema, duplicate rows, thread_id reuse) before relying on it.

---

| Plan file | Handoff title | Handoff date | Status | Telegram topic | message_thread_id | Auto-start | Resume tracker state | Last manifest update | Discord channel id | Discord thread id |
|-----------|---------------|--------------|--------|-----------------|--------------------|---|----------------------|----------------------|----------------------|----------------------|
| `plans/SUNMINT_PLOT_EXPLORER_PLAN.md` | **SunMint Plot Explorer** — a dedicated full-viewport exploration surface for SunMint plots/trees/media (`/sunmint/plots/`) instead of crowding the `truesight.me/sunmint` marketing page; the page's in-page map shrinks to a teaser linking through. Includes a **mandatory refresh-on-ingest** requirement for the prebuilt per-plot media index. | 2026-09-19 | **draft — awaiting kickoff** | (this thread 33323) | 33323 | no | **2026-09-19: Created by Sophia** at Gary's request (thread 33323). Gary's framing: the marketing page is already busy, and plot/media exploration is an *instrument* not a *story* → dedicated surface. Repo **TrueSightDAO/truesight_me_beta** (site) + **sunmint** (per-plot media index generator) → beta.truesight.me (beta-first). Reuses existing data: `sunmint/plots/index.geojson`, `sunmint/trees/index.geojson`, `farm_media_manifests/*`, and the shipped nearest-location join (farm-media-daemon #30). Only genuinely NEW artifact = a per-plot media index. **§6a (governor directive 2026-09-19): the index MUST refresh whenever underlying data changes** — reuse the proven `farm-media-publisher` systemd-timer idempotent-reconcile pattern (verified active on the box, 15-min cadence, `unchanged` output), reconcile over the FULL input set (manifests ∪ plots geojson ∪ nearest-location cache), emit a `generated_at` freshness stamp, add a staleness monitor, and assert non-staleness in UAT. A stale prebuilt index is worse than none — it re-opens the "uploaded but not published" class one layer down. §7 ONE PR PER TURN: PR0 (this plan + row) → PR1 (per-plot media index generator + freshness machinery) → PR2 (explorer shell) → PR3 (filters + counts) → PR4 (detail panel: gallery + tree list + provenance links) → PR5 (deep-links) → PR6 (marketing teaser + link) → PR7 `gate: UAT` → prod promote only on governor GO. §5 data-quality rule: surface "unclassified / no media / no farm" as explicit visible states, never hide rows. RESUME HERE = **PR1**. | 2026-09-19 |  |  |
| `plans/PII_EVENT_ENVELOPE.md` | **PII Event Envelope** — encrypt PII-bearing events at the emitter (to an operator/DAO public key) + emit a SHA-256 commitment in the clear, so submission is *verifiable without disclosure*; defuses §11.2's "privacy by location" load-bearing consequence. | 2026-09-18 | **completed 2026-09-20** — design doc + CRF plan §11.10 cross-ref + handoff registration all shipped; no code scoped to this thread. Remaining work (E1–E5 rollout, §11.4 public-JSON exclusion) tracked in the CRF plan + OPEN_FOLLOWUPS.md, not here. | (this thread 31842) | 31842 | no | **2026-09-20: thread CLOSED + ARCHIVED** per the standing closeout instruction — the design doc, the §11.10 cross-ref and the handoff registration all shipped; no code was ever scoped to this thread (§11.10 is the durable home of the workstream). Remaining open items tracked in `OPEN_FOLLOWUPS.md`: the §11.4 public-JSON `excluded_pii_events` safety confirmation + the parked E2–E5 design work. **2026-09-18: Created by Sophia** at Gary's request (thread 31842), spun out of the CRF payout work (thread 30026). Gary refined the boundary (2026-09-18): encryption applies at the **public-JSON-repo** layer (col G intake + private sheet stay plaintext) → **no browser key custody and P4 is NOT forked**; `cfr-anapu#11` proceeds independently. Hard parts open: key custody (Fernet vault vs AWS KMS), escrow/recovery, multi-recipient, signing≠encryption key separation. Pins its §11.10 in the CRF plan. | 2026-09-20 |  |  |
| `handoffs/MEDIA_GALLERY_PUBLISHER_PLAN.md` | **Media Gallery Publisher** — make farm/program media galleries a derived artifact the page *fetches* from the machine-owned `farm_media_manifests` repo, so "uploaded but not published" (the Cacau na Veia PR6 near-miss: 33 videos uploaded, page never wired) is impossible by construction. Publisher = manifest/commit stage + systemd timer, NOT the daemon. | 2026-09-13 | **COMPLETE — PR1–PR7 ✅ merged; PR7 prod deployed + verified 2026-09-13** | (this thread 26438) | 26438 | yes | **2026-09-11: Created by Sophia** at Gary's request (thread 26438), spun out of the Cacau na Veia PR6 near-miss. §5a ONE PR PER TURN: PR1 (this plan + row) ✅ merged #1036 → PR2 ✅ SUPERSEDED (the "422 sha wasn't supplied" blocker was stale: `upload_file_to_github` has fetched+passed the blob sha since truesight_autopilot #87, 2026-06-03; **re-confirmed 2026-09-11 on the real cross-repo target `farm_media_manifests/index.json`** — no-op write returned action:"updated", commit 818766d, no 422) → PR3 ✅ merged (farm-media-daemon #25) → PR4 ✅ merged (farm-media-daemon #26) → PR5 publisher + systemd timer → PR6 site fetch-first (beta, UAT gate) → PR7 prod on GO. Design: write to farm_media_manifests (no site-repo writes → no drift), fallback to ./media.json (zero-breakage). No prod sync without GO. **2026-09-13: PR5 ✅ (farm-media-daemon #27 — publisher + systemd timer; #28 — image gating Option A); publisher timer INSTALLED + enabled on the box (15-min cadence, first run exit 0, reconciled 16 collections → 15 gallery files in `farm_media_manifests`, cacau-na-veia-pacaje idempotent/unchanged). PR6 ✅ (agroverse_shop_beta #322 — `js/media-gallery.js` fetch-first, fallback + curation merge). UAT PASSED for cacau-na-veia-pacaje: 33 videos + 12 images, 12/12 image URLs HTTP 200 on beta, headless render 12 imgs + 33 iframes, page 200. PR7 = prod promote, GATED on explicit governor GO. **2026-09-13 (later): governor GO in-thread → PR7 ✅ done.** `sync_beta_to_prod("agroverse_shop_prod")` (merge-upstream, no force; ledger `deploy_20260913T020855Z_agroverse-shop-prod`); prod `main` = fetch-first `js/media-gallery.js` (`ed3c3b34b`, md5 `d6cf69f8…` = beta). Verified live on prod: asset serves fetch-first, farm page 200, headless render 12 imgs + 33 iframes, gallery 45 entries (33 video + 12 image). **PLAN COMPLETE.** | 2026-09-13 |  |  |
| `handoffs/CRF_ANAPU_MEDIA_TASK_PLAN.md` | **CRF Anapu Media Task Plan** — ingest the 2026-09-09 CRF Anapu site-visit media (8 HEIC + 14 MOV + 2 PNG, 24 files, 22 geotagged) into MAP + add a photo/video gallery to the live program page https://beta.truesight.me/programs/crf-anapu/. | 2026-09-11 | **build complete — awaiting governor UAT** (PR1–PR5 all merged/done; video-embed sizing bug found + fixed on both `beta.truesight.me/programs/crf-anapu/` and `cfr.truesight.me` 2026-09-15) | (this thread 25181) | 25181 | no | **2026-09-11: Created by Sophia** at Gary's request (thread 25181), instantiated from `handoffs/FARM_MEDIA_TASK_PLAN_TEMPLATE.md`. §0/§2 recon (all live): manifest `crf-anapu-para.json` **absent**, `farm-media-raw/crf-anapu\|jedielcio` **404**, no sidecars/transcripts in `~/cfr_work/`, no plot in `sunmint/plots/index.geojson` — **the batch is RAW, not MAP-processed**. Media: `/home/ubuntu/cfr_work/` = 8 HEIC + 14 MOV + 2 PNG, single visit 2026-09-09, Anapu/Pará, bbox −3.38970..−3.38890 × −51.30080..−51.30000 (~0.79 ha, centroid −3.38925/−51.30040); 58 frames + 16 sheet crops derived. Separate older `jedielcio` inbox batch = 4 mp4 (yt_ids set) but **no manifest**. §5a ONE PR PER TURN: PR1 (this plan + row) ✅ → PR2 manifest v2 (+ file missing `jedielcio.json`) → PR3 plot/sheet **GATED** (CRF has NO CEPOTX site code; school ≠ cacao farm) → PR4 program-page gallery (**new pattern**) → PR5 raw photos + MOV→MP4 + transcription + daemon → UAT gate. **Open decisions:** (a) farm_id `crf-anapu-para` vs `jedielcio`; (b) school-vs-farm plot semantics; (c) gallery on a *program* page (normally a farm-page convention). No prod sync without GO. **2026-09-11 (later): standalone subdomain `cfr.truesight.me` stood up & LIVE** — repo `cfr-anapu` was pre-existing + already in `allowed_repos`, so the real blocker was **Pages auto-enable** (PAT lacks the Pages scope; `configure-pages enablement:true` 403'd) — **solved by pushing a `gh-pages` branch**; Route53 CNAME → `truesightdao.github.io`; site 200 with valid Let's Encrypt cert. **Independent of the MAP media long pole (PR5), which remains in flight.** **2026-09-12: PR5 media long pole DONE** — all 14 videos uploaded, 14/14 sidecars carry `yt_id`, caption backfill done, `truesight_me_beta` PR #377 landed the gallery update. **2026-09-15:** RESUME HERE pointer was stale (still said PR5 in flight) — fixed via agentic_ai_context PR #1126. UAT surfaced a real bug: YouTube embeds rendered squished on both `beta.truesight.me/programs/crf-anapu/` and `cfr.truesight.me` (`.farm-video{height:auto}` has no effect on an `<iframe>`, no intrinsic aspect ratio) — fixed + merged: `truesight_me_beta` #378 (16:9 box) + #379 (lazy-load), `cfr-anapu` #3 (ported fix, synced `main`→`gh-pages`). Both surfaces verified live. **RESUME HERE = governor UAT** on `beta.truesight.me/programs/crf-anapu/` and `cfr.truesight.me`; no prod sync without explicit GO. | 2026-09-15 |  |  |
| `handoffs/SITIO_TORRES_MEDIA_TASK_PLAN.md` | **Sítio Torres (Pacajá loc3) Media Task Plan** — run the Media Archive Pipeline over the 2026-09-09 loc3 site-visit media (38 HEIC + 35 MOV + 8 PNG, 2.1 GB): commit the missing `farm_media_manifests/sitio-torres-pacaja-para.json`, verify the already-registered SunMint plot `N-06-66`, push photos to `farm-media-raw`, MOV→MP4 + GPS re-inject, commit the 35 existing transcripts, daemon upload (~6/day). | 2026-09-10 | completed — prod deployed & verified (PR1–PR6 + gallery wiring + prod sync `deploy_20260910T213209Z`) | (this thread 24442) | 24442 | no | **2026-09-10: Created by Sophia** at Gary's/envoy's request (thread 24442), same pattern as loc1 `CACAU_NA_VEIA_MEDIA_TASK_PLAN.md`. §5d pre-flight (all re-measured live, not carried over): media `/media/pacaje_work/loc3/` = **38 HEIC + 35 MOV + 8 PNG**, 73 EXIF-bearing, **72 geotagged (98.6%)**, 59 unique pts, single visit 2026-09-09, lat -3.54020..-3.52880 / lon -51.14554..-51.14330, hull **19.14 ha**, centroid -3.53487/-51.14476; **transcripts DONE** (35 `.json` + 35 `.vtt` in `loc3_tx/MAP/`; 26 with text, 9 silent). **Step Zero: NO duplicates** (0 intra-set dup hashes; 0 overlap with loc1) and plot `N-06-66` ALREADY REGISTERED in `sunmint/plots/index.geojson` (farm_id `sitio-torres-pacaja-para`, bbox identical to the walk bbox) → PR3 verify-only. Farm page + `media.json` (hero + 12 gallery) already live on beta & prod. **MISSING:** manifest JSON (404), `farm-media-raw/<farm>/photos/`, daemon inbox dir/entry, S3 `raw/` prefix. §5a ONE PR PER TURN: PR1 (this plan) ✅ → **PR2 (manifest v2 + index.json)** → PR3 (verify sheet row) → PR4/PR5 expected NO-OP → PR6 (HEIC → farm-media-raw, MOV→MP4 + GPS re-inject, commit transcripts, sidecars, daemon upload) → UAT gate; no prod sync without GO. **Open gates:** (a) site code `N-06-66` is *assumed* — outside the observed COOPCAO roster range, needs CEPOTX registry confirmation; (b) **provenance mismatch** — the `..._location_2_nursery.zip` holds IMG_9562–9620, DISJOINT from loc3's IMG_9622–9705, so it is NOT loc3's source; (c) claimed 4.5 ha vs walked 19.14 ha hull. **✅ COMPLETED 2026-09-10:** PR2–PR6 + gallery wiring all done; beta verified 47/47; governor GO (👍 thread 24442) → prod promoted via `sync_beta_to_prod` (merge-upstream, no force; merge `bc84be405`, deploy `deploy_20260910T213209Z`); **live agroverse.shop page HTTP 200 with 47 gallery entries (12 image + 35 youtube)**, 35/35 videoIds embeddable, 12/12 images 200, prod≡beta. Thread 24442 can close. | 2026-09-10 |  |  |
| `handoffs/GAS_DEPLOY_ACCESSOR_GUARD_PLAN.md` | **GAS Deploy Accessor Guard** — pre-push survivability check so a `clasp push` can never leave a required secret accessor (`setApiKeys`/`getCredentials`, contract derived from the tracked `Credentials.sample.js`) undefined in the live GAS project. | 2026-09-10 | **in progress** — PR #470 open + verified (8/8 unit tests, no false positive vs live `19Wag9x`); cosmetic ruff-format polish + merge remain. | [GAS accessor guard](https://t.me/c/3919341801/24846) | 24846 | yes | **2026-09-10: Created by Sophia** at Gary's request (split from thread 23408). Repo **TrueSightDAO/tokenomics** (scripts only — no deploy). Root cause: `Credentials.js` is gitignored AND `.claspignore`d, so a clean checkout lacks it; `.claspignore` only blocks deletion (never creates) and `deploy_gas_project.py` never checks existence → a `clasp push --force` silently reintroduces `ReferenceError: setApiKeys is not defined` (the 2026-09-10 v11 outage). §5a ONE PR PER TURN: PR1 (#470 guard + 8 unit tests) → merge. **RESUME HERE = land the ruff-format polish onto branch `fix/gas-deploy-accessor-survivability-guard` via `upload_local_file_to_github` (this box's `gh` token lacks write to tokenomics; the tool's creds have it) → `git ls-remote` to confirm head advanced → re-verify → `merge_pr(tokenomics, 470)` → remove `/tmp/tok_review` worktree → optional flip the OPEN_FOLLOWUPS.md entry (PR #985) to shipped.** No deploy, no ledger write. | 2026-09-10 |  |  |
| `plans/SOPHIA_GITHUB_RATE_LIMIT_GOVERNOR_PLAN.md` | **Sophia GitHub Rate-Limit Governor** — bounded outbound GitHub concurrency (semaphore) + proactive rate-limit awareness + thread-safe shared state, so parallel threads stop exhausting the write PAT. | 2026-09-05 | **new — awaiting kickoff** | — | — | no | **2026-09-05: Created + committed to main by DeepSeek Local**, at Gary's request. Repo **TrueSightDAO/truesight_autopilot** (Sophia's own codebase — opens PRs, human merges). §5d pre-flight captures: thread-concurrency already shipped (`SOPHIA_THREAD_CONCURRENCY_PLAN.md`), `_run_tool`→`asyncio.to_thread` (main.py:2484), adapter `_thread_dispatch_locks` (telegram_adapter.py:65), `_read_cache` (github_tools.py top), deploy-ledger sync httpx (deploy_ledger.py:60–179), no existing global semaphore. **§5a ONE PR PER TURN:** PR1 (bounded GitHub semaphore + thread-safe cache) → PR2 (proactive rate-limit awareness) → PR3 (bound lock registry + shared-state audit). RESUME HERE = **PR1**. | 2026-09-05 |  |  |
| `handoffs/SANTA_ANA_BAHIA_MEDIA_TASK_PLAN.md` | **Santa Ana (Bahia) Media + Plot** — archive Fazenda Santa Ana Bahia site-visit media (151 MOV + 91 HEIC, 2023-09-18, Morbeck/Coopercabruca) into media pipeline + manifest (lat/lon, creation_date, transcription), add SunMint plot FSA-P1, cross-link farm page ↔ sunmint.html both ways, extend gallery. | 2026-09-02 | executing — PR1–PR5 delivered; media long pole + loose ends remain | (this thread 19965) | 19965 | yes | **2026-09-02: Created by Sophia** at Gary's request (thread 19965). Repo mix: agentic_ai_context (plan) + farm_media_manifests + agroverse_shop_beta + truesight_me_beta + sunmint. §5d pre-flight: zip verified (151 MOV/91 HEIC/1 JPG @ 2023-09-18, GPS ~14.3227/-39.10), extraction done at /home/ubuntu/santa_ana_bahia_work, v2 manifest schema exists (oscar-bahia.json), geojson plot schema = RM-P2 pattern, media.json schemaVersion 1 (3 YT vids). §5a ONE PR PER TURN: PR1 (this roadmap) → PR2 (manifest v2 + index.json) → PR3 (farm page gallery + cross-link) → PR4 (sunmint.html ↔ farm cross-link) → PR5 (FSA-P1 plot) → gate UAT. RESUME HERE = PR1–PR5 delivered 2026-09-06 (PR2 manifest ✓; PR3 gallery + Leaflet map live prod deploy_20260906T022903Z; PR4 cross-link ✓ via FARM_SLUG fallback; PR5 FSA-P1 durably in source sheet row 14 → plots/index.geojson 13 feat, blob 5d9253d7). Remaining: media long pole (curated transcode ~15–25 + sidecars + daemon upload) + 4 loose ends (per-plot by-plot files, registry SA doc mismatch, relative sunmint.html link, Gary visual confirm). See plan RESUME HERE. 2026-09-07: daemon pipeline registered — inbox farm-media/fazenda-santa-ana-bahia added (watched); stale santa-ana-fazenda-bahia zip root removed (archive error-loop fixed); both daemons restarted clean; raws already S3-archived (151 markers). Wave-1 delivered 2026-09-07 (16 curated videos transcoded + uploaded, yt_ids committed); transcription done 2026-09-07 (16/16 faster-whisper PT, manifest @ 2093d5a). 135 videos + 91 photos remain PENDING (future waves). Loose ends #2–#4 closed 2026-09-07 (#2 skip-by-decision, #3 FIXED PR #953 e14e3eaa, #4 verified already-resolved — absolute links live in prod). Remaining: #5 Gary visual confirm → wrap-up. | 2026-09-07 |  |  |
| `handoffs/FAZENDA_SAO_JORGE_MEDIA_TASK_PLAN.md` | **Fazenda São Jorge (Bahia) Media + Plot** — archive the 2023 site-visit media (36 HEIC + 67 MOV, 103 files, 100% GPS) for the **AGL6 source farm** into MAP + the SunMint impact map: manifest `fazenda-sao-jorge-bahia.json` (v2, 103 items), plot `SJ-P1` (64.7 ha hull, cabruca — Itajuípe/Itabuna, Coopercabruca, NOT Pará), farm-page gallery + two-way cross-link (farm page ↔ sunmint.html ↔ AGL6 pledge page). | 2026-09-06 | executing — PR1–PR6 delivered (manifest 103 items: 67 videos PUBLISHED w/ yt_ids, 36 photos PENDING; SJ-P1 in the source sheet + geojson; galleries + cross-links live on beta); **UAT gate open** — governor UAT on beta surfaces, then prod sync on explicit GO | (this thread 19930) | 19930 | yes | **2026-09-06: Created by Sophia** at Gary's request (thread 19930), instantiated from `handoffs/FARM_MEDIA_TASK_PLAN_TEMPLATE.md`; the 👍 GO was given on the resume-awaiting kickoff. **Found untracked by the handoff-index reconciliation sweep (2026-09-15)** — the plan file was tracked on `main` but had no manifest/index row, so live thread 19930 was invisible to the supervisor. Repo mix: agentic_ai_context (plan) + farm_media_manifests + sunmint (api-only) + agroverse_shop_beta + truesight_me_beta. §5a ONE PR PER TURN: PR1 (plan) ✅ → PR2 manifest v2 + index ✅ → PR3 SJ-P1 plot in source sheet + geojson ✅ → PR4 farm-page gallery + cross-link ✅ → PR5 AGL6 ↔ sunmint wiring ✅ → PR6 media long pole ✅ (67/67 MP4 w/ yt_ids, farm_media_manifests#2 sha 5ae619a9; 36 HEIC → farm-media-raw/photos; inbox sidecars in place). RESUME HERE = **governor UAT** on beta surfaces (beta.agroverse.shop/farms/fazenda-sao-jorge-bahia gallery incl. 36 photos + Leaflet map; beta.truesight.me AGL6 pledge page ↔ farm page; sunmint SJ-P1 plot on impact map), then explicit GO → `sync_beta_to_prod`. No prod sync without GO. | 2026-09-15 |  |  |
| `plans/SOPHIA_INTRATURN_COMPACTION_PLAN.md` | **Exec: in-turn context compaction** — compact a single in-progress turn that needs many tool rounds (measured live: 94,264 and 72,391+ token turns), so investigation-heavy turns stop blowing the context window. Extends the now-COMPLETE inter-turn compaction system (`plans/SOPHIA_CONTEXT_COMPACTION_PLAN.md`). | 2026-09-15 | in progress — plan created (PR #1093, 2026-09-14); **Unit 0 not yet started** | [Exec: In-turn context compaction](https://t.me/c/3919341801/29305) | 29305 | no | **Found 2026-09-15 by the final gap re-check** (live thread, real plan file, non-terminal, no manifest/index row). **RESUME HERE = Unit 0** of `plans/SOPHIA_INTRATURN_COMPACTION_PLAN.md` (build the round-level compaction primitive + validate against the real 94K/72K token fixtures; never fold an incomplete tool_calls/tool_result pair). | 2026-09-15 |  |  |
| `plans/FARM_MEDIA_DAEMON_PLAN.md` | **Farm Media Daemon** — shared dumb-on-purpose YouTube upload daemon for ALL farms: sidecar metadata travels with file, global daily quota budget + round-robin fairness, GitHub FARM_MEDIA_MANIFESTS = committed source of truth; any Sophia reads/commits, governor queries any Sophia for assets. | 2026-09-01 | **complete — daemon live, all queues drained (2026-09-01)** | — | — | yes | **2026-09-01: Created by Sophia** at Gary's request (thread 17181) after Cleide's upload hit YouTube 429 quota. New repo **TrueSightDAO/farm-media-daemon**. §5d pre-flight captures: channel admin@truesight.me, upload script path, 6/day unverified quota reset ~07:00 UTC, La do Sitio 71 yt_ids committed, Cleide 71 mp4s on box, manifest convention (dict + index.json). **§5a ONE PR PER TURN:** PR0 (this roadmap) → PR1 (repo scaffold + DESIGN.md + allowed_repos) → PR2 (daemon core: sidecar upload loop + quota + fairness) → PR3 (queue reader + manifest committer CLIs) → PR4 (systemd + inboxes + migrate Cleide, retire ad-hoc uploaders) → PR5 `gate: UAT` → docs + cross-Sophia announcement. **RESUME HERE = none — COMPLETE (2026-09-01).** | 2026-09-01 |  |  |
| `plans/MEDIA_ARCHIVE_PIPELINE_DASHBOARD_PLAN.md` | **Media Archives Pipeline dashboard** — read-only, signed-in-governor view at sophia.truesight.me/media-archive-pipeline: per-farm queue status (uploaded/pending/needs_metadata/error), upload events, committed-vs-live manifest state. | 2026-09-01 | **executing — PR1–PR3 done + deployed, sentinel access live; signed-in UAT optional** | (this thread 17181) | 17181 | yes | **2026-09-01: Created by Sophia** at Gary's request (thread 17181). Repo **TrueSightDAO/truesight_autopilot** (Sophia's own code — own-repo gate, governor go authorizes self-merge of feature PRs). §5d pre-flight captures: FastAPI app on this box (nginx → truesight-autopilot.service :8001), governor auth already exists (RSA + JWT, verify_jwt 401 gate), data all local (sidecars under /home/ubuntu/media_archive_inbox, /tmp/farm_media_uploads.log, FARM_MEDIA_MANIFESTS on GitHub). **§5a ONE PR PER TURN:** PR0 (this roadmap) → PR1 (auth-gated data endpoint) → PR2 (dashboard page) → PR3 (wire + restart own service) → PR4 `gate: UAT` (signed-in verify: 3 states, live refresh, signed-out 401) → PR5 (docs). **RESUME HERE = PR4 signed-in UAT (optional) — dashboard live, core goal complete.** | 2026-09-01 |  |  |
| `plans/SOPHIA_EMOJI_REACTION_GO_PLAN.md` | **Sophia emoji-reaction GO signal** — any standard emoji reaction (except 👎) on a resume-awaiting message = go-for-it. `truesight_autopilot` change. | 2026-08-29 | new — awaiting kickoff | [Sophia emoji-reaction GO](https://t.me/c/3919341801/15728) | 15728 | yes | **2026-08-29: Created + committed to main by DeepSeek Local**, at Gary's request. Repo **TrueSightDAO/truesight_autopilot** (Sophia's own codebase). §5d pre-flight captures exact hooks (getUpdates allowed_updates, run() dispatch, send_message chunk-id gotcha, is_allowed gate, _thread_dispatch_lock/call_chat_with_progress enqueue). **§5a ONE PR PER TURN:** PR1 (receive reactions + settings.emoji_go_blocked) → PR2 (resume registry + flag ready posts) → PR3 (reaction → synthesized go-signal) → PR4 (docs). Own-repo gate (opens PRs only, never self-merges) + prod-deploy gate + UAT gate. RESUME HERE = **PR1**. | 2026-08-29 |  |  |
| `plans/SUNMINT_PUBLIC_SIGNATURES_PLAN.md` | **SunMint Public Signatures Cache** — public auditable GitHub JSON cache of ALL SunMint RSA-signed events (`sunmint/signatures.json` keyed by message ID) + public link-share of Tree Growth Measurements. | 2026-09-01 | **new — awaiting kickoff** | [SunMint public signatures cache](https://t.me/c/3919341801/17194) | 17194 | yes | **2026-09-01: Created by Sophia** at Gary's request. Repo **TrueSightDAO/sunmint** (public) + autopilot cron writer. §5d pre-flight captures signature flow, 3 sheet schemas, and the `sync_pending_caches.py` template (30-min cron, gspread → sha-aware Contents-API PUT, no PII). **§5a ONE PR PER TURN:** PR0 (roadmap) → PR1 (sync script, dry-run gate) → PR2 (cron + first publish + offline signature re-verify) → PR3 (link-share surface) → PR4 (docs + UAT). RESUME HERE = **PR1**. | 2026-09-01 |  |  |
| `plans/SUNMINT_MOBILE_APP_PLAN.md` | **SunMint Mobile App** — Capacitor-based Android + iOS wrapper (one codebase) around the existing offline-first `sunmint_beta` farmer app. New repo `TrueSightDAO/sunmint_mobile`. Android: direct signed APK. iOS: TestFlight. Zero backend changes. | 2026-08-23 | **new — awaiting kickoff** | — | — | no | **2026-08-23: Created + committed to main by Claude Anthropic**, at Gary's request. `truesight_autopilot` `allowed_repos` updated to include `sunmint_mobile` (PR #309, merged). §5d pre-flight captures the full functionality spec (from `sunmint_beta/index.html`), Edgar's backend contract, and the photo-naming convention so no PR needs cross-file discovery. **§5a ONE PR PER TURN:** PR0 (this roadmap) → PR1 (repo scaffolding, copy existing web app in unmodified) → PR2 (Android platform + smoke test) → PR3 (iOS platform + smoke test, needs a Mac) → PR4 (byte-compatible RSA signing — highest-risk item, verify first) → PR5 (native camera) → PR6 (native geolocation) → PR7 (native offline queue, SQLite+Filesystem) → PR8 (native secure storage for RSA keypair) → PR9 (sync triggers) → PR10 (branding) → PR11 (Android release signing) → PR12 (iOS TestFlight — needs governor to provision an Apple Developer account) → PR13 (docs) → **UAT `gate`** (real-device install, both flows, both platforms). RESUME HERE = **PR1**. | 2026-08-23 |  |  |
| `plans/SUNMINT_IMPACT_MAP_EXTENSION_PLAN.md` | **SunMint Impact Map Extension** \u2014 extend `beta.truesight.me/sunmint.html`: farms selector/filter, enriched plot popups (boundary authority, status, media), farms registry section, legends. | 2026-08-31 | **parked \u2014 PR0\u2013PR4 done (2026-09-01); superseded by SUNMINT_BOUNDARY_SUBMISSION_PLAN** | — | — | yes | **2026-08-31: Created by Sophia** at Gary's request. Repo **TrueSightDAO/truesight_me_beta** \u2192 beta.truesight.me (beta-first, \u00a73f). \u00a75d pre-flight captures page structure, data URLs, geojson schemas, sheet columns. **\u00a75a ONE PR PER TURN:** PR0 (this roadmap) \u2192 PR1 (enriched plot popups) \u2192 PR2 (farms selector/filter) \u2192 PR3 (farms registry section) \u2192 PR4 (legends + status styling + media) \u2192 **PR5 `gate: UAT`** (7 steps on beta) \u2192 promote to prod only with governor approval. RESUME HERE = **parked** (PR0\u2013PR4 merged; UAT+promote resume on governor request \u2014 superseded by SUNMINT_BOUNDARY_SUBMISSION_PLAN). | 2026-08-31 |  |  |
| `plans/SUNMINT_BOUNDARY_SUBMISSION_PLAN.md` | **SunMint Boundary Submission Pipeline** \u2014 farmer uploads geo-tagged boundary photos \u2192 GPS extracted (exiftool) \u2192 convex hull \u2192 plot created/updated; farm selectable before repo record exists; backend auto-creates farm on new-name boundary event. | 2026-09-01 | **complete \u2014 UAT passed 2026-09-01 (7/7); prod promoted; GAS receiver follow-on flagged** | [SunMint impact map ext](https://t.me/c/3919341801/11074) | 11074 | yes | **2026-09-01: Created by Sophia** at Gary's request (reprioritization \u2014 impact map is downstream display; this is the upstream data capture). Repo **TrueSightDAO/sunmint** (scripts) + **sunmint_beta** (farmer app) + **dao_protocol/tokenomics** (backend event). \u00a75d pre-flight captures the 5 live plots, exiftool install, runbook extraction caveats, sheet schema, and the absence of boundary UI / extract script / catalog event. **\u00a75a ONE PR PER TURN:** PR0 (this roadmap) \u2192 PR1 (`extract_plot_gps.py`) \u2192 PR2 (farmer-app \u201cLimites da Fazenda\u201d capture module) \u2192 PR3 (farms seed index) \u2192 PR4 (backend farm upsert + `FARM BOUNDARY EVIDENCE EVENT`) \u2192 **PR5 `gate: UAT`** \u2192 promote only with governor approval. RESUME HERE = **PR1**. | 2026-09-01 |  |  |
| `plans/SUNMINT_FARMER_SETTLEMENT_AND_BATCH_LINK_PLAN.md` | **SunMint farmer settlement + batch QR-tree linking** \u2014 four-item accrual ledger model (customer liability, farmer-side prepaid-asset/accrued-liability paths, settled pool) for the cash the DAO actually pays farmers/cooperatives to plant trees, plus a batch allocator (distinct-email \u2192 tree, repeat/no-email \u2192 plot) for the 520 sold QRs sitting unlinked today, plus a cross-ledger reimbursement mechanic. Supersedes the ledger-booking step of the parent plan below (\u00a77). | 2026-09-20 | **design ruled (two same-day revisions \u2014 see plan file's revision history); no code yet. RESUME HERE = PR1** | [SunMint tree-QR linking](https://t.me/c/3919341801/11596) | 11596 | no | **2026-09-20: Drafted by Envoy, revised twice same day, both real design corrections.** Draft 1: three new ledger line-items. Draft 2: simplified to "ledger only for cash," state derived from QR/SunMint row columns instead. **Draft 3 (current): reverted draft 2** \u2014 Gary caught that a farmer prepayment before confirmation is a genuine prepaid asset (real value at risk if the farmer never delivers) and a confirmed-unpaid tree is a genuine accrued liability, both standard accrual items that belong on the ledger regardless of whether cash moved at that exact instant; draft 2's "just book cash" made "paid and delivered" indistinguishable from "paid and vanished." Final model: `Cacao Tree Purchased - Not Planted` (Asset) / `Cacao Tree - To Be Paid For` (Liability, always on main ledger until paid) / `Cacao Tree Planted - Unassigned` (Asset, the settled pool) \u2014 customer side stays on its existing legacy ledger row, discharged by the link event's own independent write rather than editing live sales code. Link event can draw from either the settled pool or an unpaid-but-confirmed tree (cooperatives are slow); an unpaid unit already linked to a QR gets tagged "committed" (reusing existing `SunMint Tree Planting` columns, no new column) so its eventual payment discharges directly from that QR's own ledger, no transfer needed \u2014 the reimbursement transfer only fires when a pool unit funded by one ledger gets claimed by a different ledger's QR. Every ledger write is a signed event (six enumerated in \u00a71.5). Two governor decisions still open (\u00a70.7 automatic-vs-governor-click reconciliation; \u00a70.8 aging policy) \u2014 flagged, not blocking PR1. Verified live before writing: 520 sold QRs / 0 `ASSIGNED_TO_TREE` (`lineage-assets/qrs_index.json`), parent plan's own RESUME HERE unmoved since 2026-08-31. Concrete first real-world case to backfill once built: Gary's own already-known unbooked payout to Paulo for 10 planted trees. **2026-09-20 (later, same day): live spreadsheet + GAS audit added (§1.7–1.10).** Pulled the actual tab list (37 tabs) of `1qbZZhf…` via the Sheets API — confirmed `SunMint Tree Planting` already has `Linked QR Code`/`Linked At`/`Plot ID` live, and found two strong reuse targets nobody had connected yet: `Asset Receipts` (+ its GAS project `asset_receipt_ingest`, script `1o2lzpdT…`, + the already-existing `report_asset_receipt.py` CLI) fits `[TREE PURCHASE EVENT]` almost exactly — likely **zero new code** for v1 if the Currency allowlist isn't restrictive; and the live `payouts` tab (already has a `tree_planting_id` column!) in the `qr_code_web_service.js` project is the natural generalization target for `[FARMER PAYMENT EVENT]`, pending one open check — does its handler book real ledger legs or only log. Also caught a real doc/code discrepancy: `GAS_SCRIPT_PROPERTIES.md` claims `[TREE PLANTING EVENT]`/`[TREE PLANTING REJECT EVENT]`/payout routing entries exist in `dao_protocol`'s `dispatch.py`; the live file has none of them — flagged for a docs fix (PR9), separate from this plan. New columns needed, confirmed against live schemas: `Agroverse QR codes`."Linked Plot ID", `SunMint Tree Planting`."Payment Event Ref" — nothing else. Four SCHEMA.md sections (`SunMint Tree Planting`, `Asset Receipts`, `payouts`, `SunMint Plots`) are live, actively-written tabs with **zero** SCHEMA.md documentation today — PR1 adds them. | 2026-09-20 |  |  |
| `plans/SUNMINT_TREE_QR_LINKING_PLAN.md` | **SunMint \u2192 QR tree-link** \u2014 dapp `link_tree_planting.html` links a sold cacao QR to a SunMint tree submission (RSA-signed `[TREE PLANTING LINK EVENT]`), plus the reject/invalid path (`[TREE PLANTING REJECT EVENT]`). | 2026-08-18 | **active \u2014 reject/invalid UAT done (2026-08-31); first real LINK pending Gary go** | [SunMint tree-QR linking](https://t.me/c/3919341801/11596) | 11596 | no | **2026-08-20: Sophia (thread 11596).** UX conformed + dropdown pickers (**dapp_beta #63** merged, live on beta); reject/invalid backend (**tokenomics #396** merged, `clasp push`ed to GAS `1UrBg\u2026`); dapp Mark-Invalid control (**dapp_beta #64** merged); **public-cache rewrite** (settings wall removed \u2014 page now reads `lineage-assets/sunmint_pending.json` + `sold_pending_tree.json`, **dapp_beta #66 open**; cache generator `scripts/sync_pending_caches.py` pushed, caches live with 13 pending / 520 sold, cron refresh every 30 min on the autopilot box). RESUME HERE = **first real link (needs Gary's explicit go \u2014 books ledger + emails owner)**. **2026-08-31 (Sophia, thread 16362): reject/invalid path FULLY proven end-to-end** \u2014 061 + 487 invalidated & removed from `index.geojson`; plant \u2192 live \u2192 invalidate \u2192 gone E2E on my own test tree; auto-dispatch proven for both `[TREE PLANTING EVENT]` (row landed in 3 s) and `[TREE PLANTING REJECT EVENT]` (rows 11963/64) via dao_protocol ROUTING #149/#150 (box pulled + restarted); `#treeIdManual` free-text input REMOVED on prod (sunmint_beta #48 \u2192 sync_beta_to_prod); `TGM_GITHUB_TOKEN` SET (2026-08-31) \u2014 see GAS_SCRIPT_PROPERTIES.md (agentic_ai_context #846); reusable submit script truesight_autopilot #353 merged. | 2026-08-31 |  |  |
| `plans/FARM_SHIPMENT_MEDIA_JSON_PLAN.md` | Farm & Shipment Page Media Externalization — per-page `media.json` replaces hardcoded `<img>`/YouTube `<iframe>` on `farms/*` and `shipments/*` pages, so adding a photo/video becomes "upload asset + one JSON entry", no HTML edit. Fixes a live copy-paste bug along the way (`shipments/agl4`'s farmer-photo pointed at `agl14.avif`). | 2026-08-20 | **parked GO-ready** | [Farm/Shipment Media JSON](https://t.me/c/3919341801/11590) | 11590 | no | **2026-09-15 (Sophia): PLAN COMPLETE — PR0–PR8 ✅.** Loader `js/media-gallery.js` + all 27 farm/shipment pages migrated to per-page `media.json` (verified 1:1 on `agroverse_shop_beta` main); PR7 retired the agl6 meta-tag pattern; **PR8 = agentic_ai_context #1132 merged** (§8 in `AGROVERSE_SHOP_PAGE_CONVENTIONS.md`, documenting the current fetch-first schema + tracker correction). Superseded/extended by `FARM_MEDIA_DAEMON_PLAN.md` (✅ COMPLETE 2026-09-13, prod promoted) + agroverse_shop_beta #322. **RESUME HERE = PR9 (UAT on beta — always-stop gate).** *Row restored 2026-09-15: it had been glued onto the SUNMINT row by commit 6a3ecb7, which made thread 11590 mis-inject as the SUNMINT handoff.* **2026-08-20: Created + committed to main by Claude Anthropic**, at Gary's request, for handoff to Sophia. Repo **TrueSightDAO/agroverse_shop_beta** → beta.agroverse.shop (**beta-first, §3f — prod promotion is Gary-only and out of scope for this plan**). §5d pre-flight captures full media inventory (7 farm + 11 shipment pages) and reusable CSS/JS precedent so no PR needs cross-file discovery. **§5a ONE PR PER TURN:** PR0 (loader `js/media-gallery.js` + pilot `farms/oscar-bahia`) → PR1 (pilot `shipments/agl4`, fixes the live bug) → PR2/PR3 (remaining farm pages, batched) → PR4/PR5/PR6 (remaining shipment pages, batched) → PR7 (`shipments/agl6` — retires an older one-off meta-tag video pattern) → PR8 (doc update in this repo's `conventions/AGROVERSE_SHOP_PAGE_CONVENTIONS.md`) → **PR9 `gate: UAT`** (7 steps on beta). No prod, no money, no default-branch self-merge anywhere in scope. RESUME HERE = **PR9 (UAT on beta — always-stop gate)**. | 2026-09-15 |  |  |
| `plans/WHITE_LABEL_IMPLEMENTATION_PLAN.md` | **White-label corporate gifting — Phase 2** (state repair + visual placement). **⚠️ Runner = the `nelanco-claude` interactive box, NOT Sophia** — this row is an index entry, not a Sophia parking. | 2026-07-15 | **GO-ready — blocked on Gate D** (`NELANCO_CLAUDE_CODE_BOX_PLAN.md`: Gary must `ssh nelanco-claude` → `tmux` → `claude` login → `/remote-control`; only he can). | — (nelanco-claude runner, not a Sophia/Telegram handoff) | — | no | **2026-07-14/15: D0 + PR0 + PR1 merged by Claude Anthropic.** Repo **TrueSightDAO/agroverse_shop_beta** → agroverse.shop (**beta-first, §3f — Phase 1 violated this by shipping straight to prod**). **The funnel now works end-to-end for the first time**; suite **34/34 green**. Shipped: **PR0** ([#182](https://github.com/TrueSightDAO/agroverse_shop_beta/pull/182)) committed the implementation, which had been living **100% untracked** despite the plan claiming "deployed to production", + rebuilt the mockup from the real product photo. **D0** ([#183](https://github.com/TrueSightDAO/agroverse_shop_beta/pull/183)) — the label spec was **rotated 90°**: the physical label is **2" W × 4" H portrait** (Gary confirmed; measured 267×520px = 1:1.95) while the page asserted 4"×2" landscape in 7 places, so every design collected would have been unusable. Zero artwork existed yet, so no migration. **PR1** ([#184](https://github.com/TrueSightDAO/agroverse_shop_beta/pull/184)) — B1 (registration dead-ended into an empty card: phantom `#wl-auth-loading` write; error element trapped inside the form it reported on) + B2 (post-payment receipt destroyed ~200ms after render by a two-IIFE race → paying customers saw "No designs yet") + B14 (`cart.js` never loaded → TypeError on every page load). **§5a ONE PR PER TURN:** **RESUME HERE = PR2** (B3 shipping quoted for **stale weight** on qty change — a money bug; B4 gallery never sorts because upload uses legacy `sign()` with no `Timestamp` injection so `created_at` is always `""`; B5 shipping failures swallowed by an empty `catch`) → PR3–PR5 (placement, states A–G) → **PR6 `gate: prod deploy + UAT`**. **Verification method (mandatory — this is what found B1/B2/B4):** Playwright drives each state with stubbed Edgar/GitHub, asserting console errors + which `#wl-*` sections are visible. The pre-existing `white-label-uat.spec.ts` passed while the funnel was dead because it never fires the interaction handlers (§10's `base64ToArrayBuffer` postmortem, repeating). Per §9 every unit needs tests that **fail first**. **Use a `git worktree`** — the DeepSeek session has ~20 uncommitted files in Gary's local `agroverse_shop`. (The box's own `/opt/claude_workspace` clone is clean.) **Open for Gary:** Q1 `agroverse-designs` is a **public** repo (confirmed `visibility: public`) holding unreleased corporate branding, discoverable by hashing a customer's email; Q2 500 bars ≈ 76 lb / 1,000 ≈ 151 lb, both over the **USPS 70 lb** single-parcel limit; Q5 no 600×1200 artwork template exists (**blocks PR4**); and 2 canonical-doc corrections logged in `CONTEXT_UPDATES.md` awaiting an explicit §3 ask. | 2026-07-15 |  |  |
| `plans/LARGE_SPIKES_CARD_FIX_AND_CHART_LEGIBILITY_PLAN.md` | `/large_spikes` — disappearing company cards fix + breakout-gauge legibility (follow-up hardening of the LARGE_SPIKE_INDEX build) | 2026-07-05 | **SUPERSEDED — already implemented** (PR #1124 + #1125 merged to sentiment_importer `master` by Claude before this plan was written) | — (superseded before a topic was ever opened) | — | no | **2026-07-05: Created + committed to main by Claude Anthropic.** Repo **TrueSightDAO/sentiment_importer** (Perch/Rails → perch.truesight.me). Root cause of missing cards = **period-keyed S3 cache miss**: selector offers 1080/3600/7200/10800 but refresher only pre-generates card caches for `[7,30,90,180,360,720,1800]`; card list is fetch-only in prod ⇒ `[]` ⇒ no cards (chart survives — DB-backed). **§5a ONE PR PER TURN:** PR1 (wire the already-set-but-dead `@the_company_period`=180 into the card list so every period resolves; `large_spikes.html.erb` only + regression spec) → PR2 (breakout gauge legibility: `fill:false`→`fill:'end'` shaded over-extended band **[Chart.js v2.9.3 — do NOT use v3/v4 fill objects]** + server-rendered "Today X% · Norm Y% · <regime>" headline; `_spike_chart.html.erb` only + spec) → **DEPLOY+UAT gate** (human runs `./deploy.sh --skip-migrate`, U1–U6). No migration, no money, no data mutation — view-only, revertible. **sentiment_importer own-repo — opens PRs only, NEVER self-merges.** §5d pre-flight complete in the plan. RESUME HERE = **PR1**. **✅ Verified complete 2026-07-18** — this row's status previously disagreed with `sophia/SOPHIA_HANDOFFS.md` (which said "DRAFTED — awaiting thread + governor GO"); confirmed via `sentiment_importer` git log that PR #1124 (`bf97824f`) and #1125 (`898db920`) are both merged to `master`. See `plans/HANDOFF_REGISTRY_CONSOLIDATION_PLAN.md`. | 2026-07-18 |  |  |
| `sentiment_importer/LARGE_SPIKE_INDEX_EXECUTION_ROADMAP.md` (repo: **TrueSightDAO/sentiment_importer** — read via `read_repo_file`, NOT in the agentic_ai_context context-sync mirror) | Large Spike Index (`/large_spikes`) — bullish/momentum mirror of the sell-off index (surface breakout/leadership names like MU, INTU for continuation trades; the "other half" of `/large_dips`) | 2026-07-02 | parked GO-ready | [Large Spike Index](https://t.me/c/3919341801/8297) | 8297 | no | **2026-07-02: Created + roadmap merged to sentiment_importer `master` (PR #1098) by Claude; parked GO-ready in thread 8297.** Ports the stock-dip **column** architecture to spikes. **§5a ONE PR PER TURN:** PR1 (migration `add large_spike` + `DailyTrade.mark_large_spikes` predicate = new N-day high + `is_surge` volume + `close>sma_20>sma_100` trend gate — **`gate: eyeball predicate` with Gary, expect MU/INTU-class leaders not junk**) → PR2 (`LargeSpikeIndexRefresher` breadth ratio → `LARGE_SPIKE_INDEX` + `LargeSpikerIsFirst`/`mark_first_spike?`; wire enqueue after dip refresher at `daily_trades_importer.rb:72` + `price_refresh_completion_tracker.rb:81`) → PR3 (view data helpers: `interesting_spike_symbols_query`, `large_spikes` helper + S3 cache, `sector_distribution_for_large_spikes`) → PR4 (controller `daily_trades#large_spikes` + route + nav repoint) → PR5 (views + partials + request specs, §9) → PR6 (backfill + **`gate: prod deploy + UAT` on beta**). Reuse universe `companies.sell_off_index=true`; `first_spike` column already exists (crypto index-level use, no collision). §5d pre-flight complete in the roadmap. **sentiment_importer own-repo — opens PRs only, never self-merges.** RESUME HERE = **PR1**. | 2026-07-02 |  |  |
| `plans/MEMBERS_PAGE_SINGLE_SOURCE_PLAN.md` | Members page reliability + single-source consolidation (stop `index.json` staleness; make it the single source for Governors + Members + Sentinels so members.html reads one file) | 2026-07-01 | parked GO-ready | [Members page single-source](https://t.me/c/3919341801/8185) | 8185 | no | **2026-07-01: Created + committed to main by Claude.** Root cause: members.html reads 2 drifting caches (`index.json` for gov/members via a 6h cron GitHub drops; `dao_members.json` for sentinels, reliably refreshed). **§5a ONE PR PER TURN:** PR1 (lineage-credentials cron 6h→2h) → PR2 (treasury-cache event-driven poke on dao_members push — **gate: needs `LINEAGE_DISPATCH_PAT` secret**) → PR3 (lineage-engine: emit sentinels + `is_sentinel` into index.json) → PR4 (truesight_me_beta members.html single-source + happy-dom test — **gate: beta→prod human promotion**). PR4 depends on PR3 merged + index rebuilt. Cross-repo — opens PRs only, never self-merges. RESUME HERE = **PR1**. | 2026-07-01 |  |  |
| `plans/POST_REPACKAGING_CLEANUP_PLAN.md` | Post-Repackaging Cleanup: Auto-populate Currencies + offchain after repackaging GAS | 2026-06-28 | parked GO-ready | [Post-Repackaging Cleanup](https://t.me/c/3919341801/7987) | 7987 | no | RESUME HERE = **PR1** (`modules/post_repackaging_cleanup.py`); own-repo gate. *(Row added 2026-07-18 during registry consolidation — this handoff was registered in `sophia/SOPHIA_HANDOFFS.md` but had never been added to this manifest; see `plans/HANDOFF_REGISTRY_CONSOLIDATION_PLAN.md`.)* | 2026-07-18 |  |  |
| `plans/GOVERNOR_SHEET_PERMISSION_SYNC_PLAN.md` | Governor Sheet-Permission Sync (auto-sync Main Ledger editor list from governor roster) | 2026-06-28 | deployed | — (GAS cron, no Telegram handoff) | — | no | **2026-06-28: DEPLOYED.** GAS script `GovernorSheetPermissionSync.js` in project `1m8IZPs...`. Rule: ADD eligible (governor/sentinel with email in Contact sheet) not yet editors; REMOVE only editors IN Contact sheet who aren't governor/sentinel; KEEP everyone not in Contact sheet (GCP SAs, externals). Wired via `doGet(?action=sync_governor_editors)` + daily 04:00 UTC cron. NOT YET EXECUTED — first run adds 5 agent emails. RESUME HERE = run `syncGovernorEditorsNow()` from GAS editor to test. | 2026-06-28 |  |  |
| `sops/REVIEW_QUEUE_SOP.md` | Review Queue Standard Operating Procedure (automatable rules for LLM/Sophia review) | 2026-06-28 | draft | — (SOP document, not a Telegram-parked handoff) | — | no | **2026-06-28: Created.** Three scenarios: A=name matching (dao_members.json → lineage-credentials, exact match only), B=compute TDG from event Amount (not Grok's estimate), C=Edgar should enforce TDG server-side. Used successfully to process ~475 reviews. Future LLMs should follow this SOP for ongoing review. | 2026-06-28 |  |  |
| `plans/SOPHIA_DURABLE_JOURNAL_RESUME_PLAN.md` | Sophia Durable Journal + Checkpoint-Resume Loop (let long work resume in bounded chunks across turns; adds journal+resume on top of existing compaction) | 2026-06-23 | draft — awaiting governor review | — (draft, not parked) | — | no | **DRAFT, not parked.** §5a one PR per turn; own-repo gate (opens PRs only, never self-merges); flag-gated rollout. Pre-flight gate: **build-incremental vs adopt-a-harness**. RESUME HERE = **PR1** (`app/journal.py` primitive). | 2026-06-23 |  |  |
| `plans/QR_SELF_SERVE_CURRENCY_PLAN.md` | Self-Serve QR-Ready Currency Definition via Edgar (`[CURRENCY DEFINITION EVENT]` → QR-ready `Currencies` row, so a new SKU goes zero→QR via Edgar only) | 2026-06-23 | **COMPLETE 2026-09-13** — PR1–PR5 merged; 1N6o00 GAS deployed live (v15, `AKfycbxn3siu…` @15); UAT U1–U5 ✅ (U5 cleanup ✅) | [QR Self-Serve Currency](https://t.me/c/3919341801/27015) | 27015 | no | **Execution thread is 27015** (the row's original parking thread `7611` is not where it ran — plan §5). **§5a ONE PR PER TURN.** PR1 ✅ (`dao_protocol` #131 `f6825d0`: `define_currency` CLI + `dispatch.py` route; label set extended to 13 for col M `SKU Product ID`). PR2 ✅ (`tokenomics` #376 `f3fdfa4`: GAS handler in 1N6o00; + #476 col-M write + `Serializable` inference, #477 `skus.json` emit). PR3 ✅ **verified live 2026-09-13** — `DAO_PROTOCOL_WEBHOOK_CURRENCY_DEFINITION` in the running service env (31 webhook keys), deployment `AKfycbxn3siu2Qrz…`; probe → HTTP 200 `{"status":"success",…}`; rows 12409/12410 re-fired → both `Successfully Completed`. Edgar checkout had been 4 commits stale (`80061a2`, catalog v5/40) → `git pull --ff-only` → `3b42488` + restart (**rollback SHA `80061a26`**); public `events-catalog` now serves **v7/46** with all 4 new tags present, and `lookup_event_docs("CURRENCY DEFINITION EVENT")` resolves from the live catalog (the original "not found" symptom fixed). PR4 ✅ (`tokenomics` #489: auto-define QR-ready row inline from a `[BATCH QR CODE REQUEST]` carrying define-fields; operator opted in 2026-09-13; **deployed live 2026-09-13 — v15, `AKfycbxn3siu…` @15**). PR5 ✅ (docs: `AGROVERSE_QR_CODE_BATCH_GENERATION.md` §2b + `LEDGER_CONVERSION_AND_REPACKAGING.md` + `QR_GENERATION_DAO_CLIENT_POSTMORTEM.md` — zero→QR-ready→minted via Edgar only). **✅ COMPLETE 2026-09-13.** GAS **deployed live**: `clasp push --force` → v15, deployment `AKfycbxn3siu2Qrz…` repointed **@15**; probe → 200 `{"status":"success"}`. **UAT U1–U5 PASS** (U5 ✅: all TEST artifacts deleted). New defect surfaced: **duplicate minting under webhook retry** (Edgar retried the slow GAS 3× on 30 s read-timeout) — filed to `OPEN_FOLLOWUPS.md ## Pending`. Cross-repo gate — **opens PRs only, never self-merges**. Pre-flight (reuse deployment `AKfycbxn3siu…`; UAT sheet decision) in plan §3. | 2026-09-13 |  |  |
| `plans/SOPHIA_CONTRIBUTION_SCORING_PLAN.md` | Score the contribution review backlog (906 rows / ~90.7k TDG) | 2026-06-23 | in progress | — (register on GO) | — | no | **2026-06-28: Bulk of backlog processed** — ~475 items approved via auto-review (SOP at REVIEW_QUEUE_SOP.md), ~15 rejected, ~5 edge-case skips resolved. Remaining: verify GAS write-back completed, confirm TDG transferred to Ledger. RESUME HERE = verify transfer script ran + report results. **Note:** this row's status previously disagreed with `sophia/SOPHIA_HANDOFFS.md` (which said "draft — awaiting governor §10 + GO"); this manifest's more detailed/recent narrative is trusted per `plans/HANDOFF_REGISTRY_CONSOLIDATION_PLAN.md` — confirm with Gary if uncertain. | 2026-07-18 |  |  |
| `plans/SCORING_REVIEW_QUEUE_PLAN.md` | Scoring Review Queue (cache generator, Edgar endpoint, DApp review UI) | 2026-06-18 | completed | [Scoring Review Queue](https://t.me/c/3919341801/7191) | 7191 | no | **2026-06-28: COMPLETE.** PR-INTEGRATION fixed (URL prefix, auth via dao_members.json). Review queue processed — ~475 approvals submitted via Deep Seek RSA key, ~15 rejected (email verification events, tests), ~5 skipped (unrecognized names later resolved). Sentinels section on members.html now renders (dao_members.json expanded to 406 contributors). SOP at REVIEW_QUEUE_SOP.md. RESUME HERE = none (done). | 2026-06-28 |  |  |
| `plans/SOPHIA_DAPP_EVENT_ALIGNMENT_PLAN.md` | Sophia↔DApp Event Alignment (all event types) | 2026-06-18 | parked GO-ready | [DApp Event Alignment](https://t.me/c/3919341801/6416) | 6416 | no | Parked GO-ready in **thread 6416**. **ONE PR PER TURN (§5a):** on GO run **PR1 ONLY then STOP** (catalog merge UPDATE — `_refresh_events_catalog` updates required_fields + canonical_labels for events already in the dicts, not just adds new). Then PR2 load-timing → PR3 fallback snapshot → PR4 normalizer (flagged) → PR5 stop-dropping-keys (needs PR4) → PR6 intent→event picking (review gate) → PR7 enforce-lookup (needs PR6) → PR8 UAT. `truesight_autopilot` own-repo gate — opens PRs only, NEVER self-merges; RESUME HERE = PR1. Catalog already complete (30 events) — consumption-correctness effort. | 2026-06-18 |  |  |
| `plans/CLI_SALES_EVENT_ALIGNMENT_PLAN.md` | CLI Sales Event Audit & Alignment with DApp | 2026-06-17 | in progress | [CLI Sales Event Alignment](https://t.me/c/3919341801/6311) | 6311 | no | Parked GO-ready in **thread 6311**; on GO runs PR1 (update CLI report_sales module in dao_client) → PR2 (update Edgar docs) → PR3 (re-submit Gergana's sale); **dao_client repo gate** — opens PRs only, NEVER self-merges; RESUME HERE = PR1 | 2026-06-17 |  |  |
| `plans/PUBLIC_KEY_LOOKUP_CACHE_PLAN.md` | Public-Key Lookup Cache (content-addressed per-key governor store; retires the 5-min vault cache-lag bug) | 2026-06-16 | blocked — UAT ran 2026-09-15 (U1 ⚠️ / U2 ✅ / U3 ✅ / U4 ✅ / **U5 ❌ FAIL**: per-key emitter regression); awaiting Gary's merge + GAS deploy of tokenomics#494 | [Public-Key Lookup Cache](https://t.me/c/3919341801/30471) | 30471 | no | **2026-09-15 (Sophia): PR1–PR5 ALL MERGED / SHIPPED — `truesight_autopilot` only, NEVER self-merged.** PR1 (GAS generator emits `treasury-cache/public_keys/<sha256>.json`) + PR2 (incremental + revocation) shipped 2026-06-16; PR3 (`governor_registry.resolve_key` point-lookup, autopilot#230 + #468 tests) merged; **PR4 (vault force-fresh-on-deny, [autopilot#469](https://github.com/TrueSightDAO/truesight_autopilot/pull/469), sha `23266c0`, Gary-reviewed + merged) — `resolve_key_fresh()` reads the per-key file via the authenticated contents API and an interactive sign-in does ONE fresh lookup on deny before refusing.** **PR4 is MERGED but NOT DEPLOYED** — deploying `truesight_autopilot` is a genuine always-stop gate; **UAT U2 (new key recognised on first sign-in) is pending Gary's deploy.** track_registry hold VACATED. **PR5 step 1 ✅ merged** — key→name point-lookup consumers migrated to `resolve_key` ([autopilot#470](https://github.com/TrueSightDAO/truesight_autopilot/pull/470), sha `5f7b941`, Gary-reviewed + merged); §3 consumer audit recorded there. **PR5 step 2 ✅ merged** — DApp `permissions.js` per-key resolve ([dapp_beta#99](https://github.com/TrueSightDAO/dapp_beta/pull/99), sha `345c416`, Gary-reviewed + merged). **PR5 step 3 = evaluated, NOT NEEDED** (no UI needs cheap governor enumeration — audit recorded in the plan). **PR5 IS COMPLETE ON BETA; remaining are GATES.** **BLOCKING `dapp_prod` promotion — Elizabeth Wong per-key `roles` mismatch:** her `public_keys/<sha256>.json` says `["member"]` while `dao_members.json` says `["governor","member"]`; step 2 trusts the per-key file, so her key would be denied governor actions (root cause queued to Gary in OPEN_FOLLOWUPS). Also open: **UAT U1–U5 on beta** + **UAT U2 pending Gary's `truesight_autopilot` deploy** (PR4 merged, not deployed). RESUME = resolve blocker + UAT. ONE PR PER TURN (§5a). **UAT outcome 2026-09-15 (Sophia): U1 ⚠️ pass w/ coverage gap (84% of governor keys have a per-key file) / U2 ✅ code-path / U3 ✅ / U4 ✅ / U5 ❌ FAIL.** **U5 root-caused: the per-key emitter was dropped by migration commit `50999ec` (2026-06-16) and the store has been FROZEN since 2026-06-18** — every `dao_members.json` refresh since touches 0 per-key files; org-wide search finds no re-homed emitter. This is the SHARED ROOT CAUSE of the Elizabeth Wong drift (reframed one-off → systemic symptom). **Fix PR open: [tokenomics#494](https://github.com/TrueSightDAO/tokenomics/pull/494) (restores 3 dropped helpers + per-key/manifest block verbatim; node --check green on all 8 project .js); GAS `clasp push` + `publishDaoMembersCacheNow()` is `gate:human` — NOT deployed.** After deploy: re-run U5/U1 (expect coverage → 100%) then promote `dapp_prod`. **2026-09-16 BONUS (Envoy, while parked):** self-diagnosed + fixed a recurring stale-auto-advance-directive bug (5+ occurrences this session) — root cause was `_compute_advance_signal` reading the plan from a 300s-synced local clone instead of fresh origin. Fix `truesight_autopilot#477` (new `get_context_file_fresh()`, path-traversal guarded, git-show blob read, graceful fallback, 5 regression tests) — own-repo, self-merged within standing authority. **DEPLOY LANDED 2026-09-16T21:12:44Z** (commit `9551ed2`) — `get_context_file_fresh()` confirmed present in the deployed source. tokenomics#494 merge still blocked (Envoy's own merge attempt classifier-blocked; Gary's to do). | 2026-09-15 |  |  |
| `plans/SOPHIA_VAULT_CREDENTIAL_MIGRATION_PLAN.md` | Vault Initialization & Credential Migration (migrate /home/ubuntu/ bare credentials into encrypted vault) | 2026-06-14 | completed — UAT U1-U7 passed (3 in-scope SSH keys); krake_nginx pin fix deployed 2026-09-16; Unit 6 CLOSED — Gary ruled NOT to archive any bare *.pem (box entry + GitHub SSH depend on them); all PEMs left in place | [Vault Init & Credential Migration](https://t.me/c/3919341801/30473) | 30473 | no | Parked GO-ready in **thread 30473** (fresh 2026-09-15; old 3981 deleted — send-probe "message thread not found"). Vault **initialized 2026-06-15**, **42 credentials** present. RESUME HERE = **finish migration** — move remaining bare `/home/ubuntu/*.pem` (`NELANCO_aws_20201122.pem`, `NELANCO_california_20260213.pem`, `server_us.pem`) into the vault, repoint tools; UAT U1–U7 = gate; **own-repo gate**. **2026-09-15/16:** UAT U1-U7 passed; krake_nginx pre-existing vault-pin gap found + fixed (`truesight_autopilot#476`, host-local ed25519 pin, merged). **DEPLOY LANDED 2026-09-16T21:12:44Z** (commit `9551ed2`) — krake_nginx's `id_ed25519_truesight_autopilot` key pin confirmed present in deployed `app/tools/ssh_tools.py`. Top-level `/home/ubuntu/*.pem` dups archived (safe, unreferenced). **2026-09-16T21:08Z:** Gary said "wrap up" in-thread; contribution report filed (240 min, PRs #476/#1198/#1217). Sophia correctly self-paused rather than proceed on the still-open `~/.ssh/*.pem` archiving decision (breaks 21 SSH config aliases + drops fallback safety net) — ****2026-09-16: Gary RESOLVED it — do NOT archive any bare PEM. The 3 top-level dups were restored (step (a) reverted); Unit 6 closed.**** | 2026-09-15 |  |  |
| `plans/SOPHIA_LIVE_PROGRESS_PLAN.md` | Live Progress Introspection (report what the executing turn is doing when Gary asks mid-turn, instead of a canned queued ack) | 2026-06-11 | **blocked_on_human — U3 fix `truesight_autopilot#475` deployed 2026-09-16 (commit 9551ed2); live functional re-verification of U3 still pending** | [Live Progress Introspection](https://t.me/c/3919341801/30475) | 30475 | no | Go-ready in **thread 30475** (fresh 2026-09-15; old 2799 deleted — send-probe "message thread not found"). Build **DONE** — `_live_progress` + cross-process fix **#241 merged + deployed 2026-06-17**; fix **#467 merged + deployed 2026-09-15** (classify on raw user text, not the handoff-decorated text). **UAT U1–U5 ✅ passed against the deployed build 2026-09-15** (U2/U4 re-verified post-#467, incl. cross-process `GET /chat/progress` → `running:true`+snapshot while the turn held the lock) — but a **fresh re-verification after PR2 (`truesight_autopilot#474`) advanced the deploy found U3 actually FAILS**: the progress-query classifier's bare `(progress|status|update)` alternation matched any short message merely containing those words, silently dropping real instructions sent during a busy turn (e.g. "please also update the README…"). Fix **`truesight_autopilot#475`** (anchor regex to the whole message; 23 new tests) reviewed personally by Envoy and merged (`5738db7`). **DEPLOY LANDED 2026-09-16T21:12:44Z** (commit `9551ed2`) — the anchored `_PROGRESS_QUERY_RE` confirmed present in deployed `app/telegram_adapter.py` (source-level, not yet a live functional re-test). RESUME HERE = **re-verify U3 live** (a real instruction sent mid-turn must not be misclassified as a status ping), then close out. **own-repo gate honoured: PRs only, never self-merged.** | 2026-09-15 |  |  |
| `plans/SOPHIA_MULTI_TENANT_GOVERNANCE_PLAN.md` | Sophia Multi-Tenant Governance, Identity & Vault — **PHASE 0 only** (policy layer + tool-layer auth + data-vs-instruction boundary + guest-default) | 2026-06-11 | blocked | [Multi-Tenant Governance — Phase 0](https://t.me/c/3919341801/2744) | 2744 | no | Parked GO-ready in **thread 2744**; on GO runs **Phase 0 only** (RESUME HERE = PR0.1 `app/policy.py`), then STOPS for human merge + Phase 0 UAT (P0.1–P0.4) before Phase 1; **own-repo gate: opens PRs only, NEVER self-merges**; do NOT enter any collaborative group until Phase 0 ships | 2026-06-11 |  |  |
| `plans/SOPHIA_FOLLOWUP_MONITOR_PLAN.md` | Durable Follow-up Monitor (thread-bound, multi-day; gmail_reply + elapsed_days; on strike spins a Sophia turn in the thread) | 2026-06-11 | in progress | [Follow-up Monitor](https://t.me/c/3919341801/2622) | **NEEDS VERIFICATION** (registry had `2622`, reused across 3 plans — see PR) | no | Parked GO-ready in **thread 2622** *(as recorded — needs confirmation, see thread_id note)*; Gary gave GO 2026-06-11; on GO runs PR1→PR4 (RESUME HERE = PR1 `app/followups.py`); **own-repo gate: opens PRs only, NEVER self-merges**; UAT U1–U9 is the completion gate | 2026-06-11 |  |  |
| `plans/AUTOPILOT_HARDENING_PLAN.md` | Autopilot Hardening (Sophia's own codebase) | 2026-06-10 | superseded | [Autopilot Hardening](https://t.me/c/3919341801/2317) | 2317 | no | **SUPERSEDED 2026-09-15 (stale-topic audit).** Thread 2317 deleted; Phase-1 PR-A + PR-B (full pytest in CI, no `--deselect`; ruff lint+format) **already landed** — only **`mypy` (PR-C)** remains, folded into a standalone `OPEN_FOLLOWUPS.md` entry rather than a handoff topic. No reopen. | 2026-09-15 |  |  |
| `plans/BETA_SANDBOX_ENDPOINT_PLAN.md` | Beta Sandbox Endpoint (beta.edgar.truesight.me) | 2026-06-09 | superseded | [Beta Sandbox Endpoint](https://t.me/c/3919341801/1955) | 1955 | no | **SUPERSEDED 2026-09-15 (stale-topic audit).** Thread 1955 deleted; **`beta.edgar.truesight.me` is LIVE** (FastAPI `/docs`+`/ping` 200; `ssh_key_dao_protocol_beta` in vault) — Units 1–5 built; consumer work now tracked in `CHOCOLATE_SUBSCRIPTION_PLAN.md`. No reopen. | 2026-09-15 |  |  |
| `plans/CHOCOLATE_SUBSCRIPTION_PLAN.md` | Agroverse Chocolate Subscriptions — Phase 1 | 2026-06-09 | blocked | [Chocolate Subscriptions (Phase-2 E2E)](https://t.me/c/3919341801/30477) | 30477 | no | Parked GO-ready in **thread 30477** (fresh 2026-09-15; old 1939 deleted — send-probe "message thread not found"). Phase 1 **shipped on `agroverse_shop_beta`** (generic subscribable SKU, `js/subscribe.js`, `/subscribe/chocolate-bar/` live 200). RESUME HERE = **Phase-2 webhook→fulfillment E2E** (sandbox `beta.edgar.truesight.me` now live); STOP at operator test gates | 2026-09-15 |  |  |
| `plans/RESEND_VERIFICATION_PLAN.md` | Resend verification email | 2026-06-08 | in progress | [Resend Verification](https://t.me/c/3919341801/2622) | **NEEDS VERIFICATION** (registry had `2622`, reused across 3 plans — see PR) | no | PR1 merged + deployed; PR2 (oracle) next | 2026-06-08 |  |  |
| `handoffs/SANDBOX_THEOBROMA_1_HANDOFF_DEMO.md` | THEOBROMA-1 (cacao brew demo) | 2026-06-07 | demo · live | [THEOBROMA-1 Demo](https://t.me/c/3919341801/2622) | **NEEDS VERIFICATION** (registry had `2622`, reused across 3 plans — see PR) | no | — | 2026-07-18 |  |  |
| `handoffs/CACAU_NA_VEIA_MEDIA_TASK_PLAN.md` | **Sítio Cacau na Veia (Pacajá) Media + Plot** — mature cacao agroforest plot **N-06-37** + restoration sub-plot `N-06-37_20260909_restoration_1` on `agroverse.shop/farms/cacau-na-veia-pacaje/` and the SunMint impact map, cross-linked both ways; 33 MOV + 30 HEIC 2026-09-09 site-visit media through MAP. | 2026-09-10 | ✅ **COMPLETED 2026-09-11 — prod deployed & verified (PR1–PR6; deploy `deploy_20260911T145241Z`) — thread can close** | [Cacau na Veia (Pacajá)](https://t.me/c/3919341801/24441) | 24441 | yes | **2026-09-10: Created by Sophia** at Gary's request (thread 24441). Repo mix: agentic_ai_context (plan) + farm_media_manifests + sunmint (api-only) + agroverse_shop_beta. §5a ONE PR PER TURN: PR1 plan → PR2 manifest `cacau-na-veia-pacaje.json` (64 items) → PR3 SunMint Plots sheet rows (N-06-37 30 ha + restoration 0.74 ha) + `plots/index.geojson` 22 feat (blob `8d3434c3`) + `farms/index.json` 15 farms (blob `10ad6004`) → PR4 farm page (`agroverse_shop_beta` **PR #310**, merged `4662efc8`; live on beta) → PR5 **verified NO-OP** (geojson `farm_id` == page slug ⇒ `sunmint.html` line 862 fallback links back automatically) → PR6 media pipeline. **✅ COMPLETED 2026-09-11:** PR6 done + verified — manifest v2 (64 items, **33 yt_id**); site `media.json` gallery 12→**45** (33 yt + 12 img); governor GO → prod promoted via `sync_beta_to_prod` (merge `a72f95ad`, deploy `deploy_20260911T145241Z`); live agroverse.shop page 200, gallery 33 yt + 12 img; CNAME preserved. loc1 transcription ALL_DONE (33 json + 33 vtt). Thread 24441 can close. No money, no prod. | 2026-09-11 |  |  |
| `plans/DISCORD_ADAPTER_PLAN.md` | **Discord Adapter** — Sophia speaks natively in the TrueSight DAO guild as its own process (`truesight-autopilot-discord.service`); identity SSOT = Main Ledger → *Contributors contact information* → col G "Discord ID"; fixed persona; governor allowlist. | 2026-09-12 | deployed — Tier‑2 parity gaps in progress | [Discord Adapter](https://t.me/c/3919341801/27138) | 27138 | no | **2026-09-12: Created by Sophia** at Gary's request; four policy decisions locked (§0). **2026-09-13: adapter LIVE** (`truesight_autopilot` #428 — adapter + systemd unit + 18 tests; `DISCORD_DRY_RUN=false`, 5/5 services active, guild `923008087315587072`, allowlist=[Gary]). **2026-09-14:** MEMBER tier (#440); Tier‑1 Telegram‑parity COMPLETE + UAT‑confirmed (real governor turn 15:28 used attachment ingest + progress/edit); **Tier‑2 #6 reaction‑based go‑signal merged (#454, CI green)**. Parity plan: `plans/DISCORD_TELEGRAM_PARITY_GAPS.md`. **Next:** Tier‑2 #7 `/verify` binding · #4 voice I/O · #5 per‑channel always‑respond override. **Gated on governor GO:** **Deployed 2026-09-14 17:36Z** via detached `systemd-run` — HEAD `76304fd`→`e45a56e`; 5/5 services `active`, `NRestarts=0`; marker + ecosystem_change_logs entry written; token-leak grep clean. **Next:** Tier‑2 #7 `/verify` binding · #4 voice I/O · #5 per‑channel always‑respond override. **New finding filed:** progress-edit 429 storm (OPEN_FOLLOWUPS.md). **2026-09-14 (later): #456 per-channel dispatch lock merged + deployed + verified** — box HEAD `6e49206`; `_channel_dispatch_lock()` live (L1209); 5/5 services `active`, `NRestarts=0`. **Tier-2 #7 `/verify` RECONCILED as SUPERSEDED** (governor-locked §0 decision #3: no `/verify` DM flow — the *real* binding blocker is the already-filed col-G numeric-seeding item; governor approved). **Next:** Tier-2 **#4 voice I/O** or **#5 per-channel always-respond**. | 2026-09-14 | 923012941937250375 |  |
| `plans/DISCORD_TELEGRAM_PARITY_GAPS.md` | **Discord ↔ Telegram parity gaps** — bring the Discord adapter to feature parity with the Telegram adapter. Tracked in Discord **#discord-parity** (`1549079137480605736`). | 2026-09-15 | **in progress — code done, live UAT outstanding** — Tier-1 ✅ (#450/#447); #6 reaction go-signal ✅ (#454); #5 per-channel always-respond ✅ already built; #7 `/verify` closed won't-build; #4 voice I/O — **✅ MERGED** (`truesight_autopilot` #459, 2026-09-15 13:13:56, both directions: transcribe-in + spoken-reply-out) — **⚠️ Gary reported live voice attachments not sending, filed as a follow-up 2026-09-15**; verified the merged code is on-disk on the box and the Discord adapter service restarted onto that exact HEAD at 16:39:01 UTC, but this was observed, not deliberately deployed-and-verified — needs an actual live UAT pass (send a voice note, confirm a spoken-reply attachment actually lands), not just a timestamp coincidence trusted at face value. | — | — | no | **2026-09-15:** spun out as its own trackable row at Gary's instruction (thread 27138) so a supervisor can manage it independently of the umbrella adapter row. Design record: `plans/DISCORD_ADAPTER_PLAN.md`. **2026-09-15 (later):** Gary observed live voice-sending not working; Envoy verified #459 is merged and the on-disk code + a recent (16:39:01 UTC) clean service restart match, but did **not** personally confirm a live voice-reply round-trip — that verification is the actual remaining work, not new implementation. RESUME HERE → **live UAT: send a real (or test-tagged, §5g) voice note to the Discord adapter, confirm a spoken-reply attachment actually posts**; if it fails despite the code being deployed, diagnose why (e.g. `edge-tts`/`faster-whisper` dependency missing on the box, a runtime exception swallowed silently) rather than assuming deploy alone fixed it. | 2026-09-15 | `1549079137480605736` | — |
| `plans/DISCORD_SHOW_TYPING.md` | **Discord adapter — typing indicator** — add a background typing loop to `app/discord_adapter.py` mirroring the Telegram adapter (keep the channel's typing state fresh before `call_chat`, stop on reply). Tracked in Discord **#show-typing**. | 2026-09-15 | **go-ready** — finding filed 2026-09-14 (Discord adapter has no typing indicator); idea confirmed. Not started. | — | — | no | **2026-09-15:** given its own trackable row at Gary's instruction (thread 27138). Parent design record: `plans/DISCORD_ADAPTER_PLAN.md`. RESUME HERE → implement the typing loop (one PR) + tests, then UAT in #show-typing. | 2026-09-15 | `1548896014868676608` | — |
| `plans/DISCORD_ADAPTER_BUGS.md` | **Discord adapter — bug supervision** — standing channel for Discord-adapter (and cross-adapter) bug findings + fixes. Tracked in Discord **#adapter-bugs**. | 2026-09-15 | **in progress** — stale "...needs approval — open the DApp..." prompt string fixed in prod (2026-09-14); open: Discord resume-registry rewrite; chat-blocking timeout / 429 storm (re-referenced here from #member-tier). | — | — | no | **2026-09-15:** given its own trackable row at Gary's instruction (thread 27138). Parent design record: `plans/DISCORD_ADAPTER_PLAN.md`. RESUME HERE → the 429/timeout item + the resume-registry rewrite (one PR each). | 2026-09-15 | `1549118558955634819` | — |
| `plans/DISCORD_MEMBER_TIER.md` | **Discord adapter — MEMBER-tier interaction** — let a resolved MEMBER-role sender get a reply (`app/policy.py` has `Role.MEMBER`; `discord_adapter.handle_message` replies only to GOVERNOR today). Tracked in Discord **#member-tier**. | 2026-09-15 | **go-ready** — scoped by Gary 2026-09-15; design + implement the MEMBER reply path (one PR), then UAT. | — | — | no | **2026-09-15:** given its own trackable row at Gary's instruction (thread 27138). The chat-blocking timeout/429 discussion previously mis-filed in this channel is re-referenced to `plans/DISCORD_ADAPTER_BUGS.md`. Parent design record: `plans/DISCORD_ADAPTER_PLAN.md`. | 2026-09-15 | `962081045044424774` | — |
| `plans/BRAIN_TIER_AWARENESS.md` | **Brain tier-awareness** — make the brain (`/chat-blocking`) receive the Discord author's resolved role and apply `{guest<member<governor}` itself, so a MEMBER can get a reply without inheriting governor authority. Child of `plans/DISCORD_MEMBER_TIER.md`. Moved to Telegram (see note) — Discord **#brain-tier-awareness** (`1549762040518672419`) kept only as historical log. | 2026-09-16 | **COMPLETE — PR1–PR3 merged + deployed; UAT §7 PASSED 2026-09-17** | (this thread 30892) | 30892 | no | **2026-09-16:** created; channel #brain-tier-awareness stood up. Parent: `plans/DISCORD_MEMBER_TIER.md`. Umbrella: `plans/DISCORD_ADAPTER_PLAN.md`. PR1 (transport) ✅ merged `truesight_autopilot#478` sha `9551ed2`. **D4 locked** (`agentic_ai_context#1223` sha `187f39d`). Moved to Telegram thread 30892 (Discord non-governor messages were only logged as `observed`, never dispatched). **PR2 (#480) ✅ MERGED 2026-09-16T21:43:16Z, squash `08ea0973`** — independently verified via `gh pr view 480 --json state,mergedAt,merge_commit_sha` (Envoy, not just a chat claim, per sec6): `Role.SENTINEL` distinct from `Role.GOVERNOR` (not aliased), `has_governor_rights()` grants WRITE/ADMIN to governor-or-sentinel, `create_jwt(...,author_name)` + `author_name_from_claims()` fix the attribution hazard (`[GOVERNOR_IDENTITY:]` only injected for a true governor; a sentinel turn is attributed by its own name). Gates: compileall/ruff/format all green; 19+42 relevant tests pass; 3 full-suite failures confirmed pre-existing (reproduce identically on stashed `main`, untouched files). Blocker hit + solved: the box's HTTPS/PAT credentials can't reach the org (403 as `garyjob`) — the fix was pushing over **SSH** (`~/.ssh/id_ed25519_truesight_autopilot`, authorized as `garyjob` on GitHub), not HTTPS; `git_push_changes`'s SSH path works but was undocumented (Sophia flagged a runbook follow-up). Also hit a ~17min GitHub-API rate-limit stall (PyGithub blocking backoff on a 403, froze the single-worker service for ALL threads, not just this one) — waited it out rather than restarting; both blocked turns (this one + CRF cohort thread 30026) resumed cleanly once it cleared. **PR3 (adapter dispatch)**: code complete + gated green this turn, **push still pending** (next turn). RESUME HERE (topic 30892) → push/PR/merge PR3 (flip `if role != "governor"` in `discord_adapter.py::handle_message()` so a resolved member is dispatched read-only, not just observed) → R1/R2/R3 UAT live in topic 30892. **Gate:** identity/authority core — open PRs only, no production deploy without governor GO. **2026-09-17: PR3 ✅ merged `truesight_autopilot#481` (squash `8686762`)** — smoke+test green, `MERGEABLE/CLEAN`; also closed superseded duplicate #482. Governor GO received in-thread → **deployed** to the autopilot box (all 5 truesight services live on `8686762`, restarted 13:36 UTC; PR1+PR2+PR3 all loaded). **UAT §7 ✅ PASSED** (topic 30892): member @-mention→read-only reply (live HTTP 200), member no-mention→observed only, member WRITE (`submit_contribution`, `ssh_run`)→**blocked**, governor unchanged, guest→blocked, sentinel→allowed & self-labelled (D4); binding confirmed live (**Peter Da→member**, Discord ID `…468692` + email bound; Gary→governor). Residual follow-up filed in `OPEN_FOLLOWUPS.md` (`policy.resolve_identity()` has no SENTINEL branch — sentinel rights reach the gate only via the JWT-asserted role) via `agentic_ai_context#1250`. Contribution report submitted. **PLAN COMPLETE.** | 2026-09-17 | `1549762040518672419` | — |

| `plans/DISCORD_ENVOY_GOVERNOR_PARITY_PLAN.md` | **Sophia ↔ Envoy governor parity on Discord** — Sophia's Discord adapter drops every bot-authored message (incl. Envoy's own Discord bot) before any role check runs, so she never replies to Envoy on Discord though the equivalent works on Telegram; this carves a narrow, governor-gated exception so Envoy can reach the same authority tier it already has on Telegram, unblocking Discord-native supervision (nudges, go-signals, gate-clearing). | 2026-09-15 | **COMPLETE — PR0–PR3 ✅ 2026-09-17** | [Exec: Discord Envoy governor parity](https://t.me/c/3919341801/29970) | 29970 | no | **2026-09-15: Created + committed to main by Claude Anthropic (Envoy)**, at Gary's request, for handoff to whichever Envoy/Sophia supervisor thread picks it up next. Repo **TrueSightDAO/truesight_autopilot** (Sophia's own codebase — opens PRs, human merges). §5d pre-flight captures the exact `handle_message`/`author_role` behavior, live config state (`DISCORD_ALLOWED_USER_IDS`/`DISCORD_MEMBER_USER_IDS` both unset today), and confirms `create_/update_/delete_discord_channel` already exist (unrelated but adjacent surface). **§5a ONE PR PER TURN:** PR0 (resolve Envoy's Discord bot id + read Contributors sheet col G/X binding pattern) → PR1 (narrow `DISCORD_TRUSTED_BOT_IDS` exception in `handle_message`, regression-tested) → PR2 **`gate: human`** (grant Envoy's bot id governor role — live authority, always-stop) → PR3 (UAT U1–U5 + docs). **2026-09-17 (Gary, exec thread): CORRECTED — target tier is SENTINEL, not governor.** Envoy's Discord bot (`1548902290344255600`) resolves through the existing D4 sentinel path (sheet col W → dao_members.json → sentinel_emails() → author_role() → `sentinel`); no governor grant. **PR0 ✅ DONE 2026-09-17**: bot id resolved; sheet col G already bound + col W=TRUE (no edit needed); findings A) cache row for Envoy has `email:null` so sentinel_emails() omits him → resolves member; B) sentinels are ALREADY dispatched post-BRAIN_TIER PR3, so the plan's "must be governor" premise is stale; C) live cache is schema v3 not v4 (0 discord_id rows). PR1 = bot-filter carve-out; PR2 = resolve to sentinel via (i) env `DISCORD_SENTINEL_USER_IDS` [recommended] or (ii) fix Finding A's cache email. **RESUME HERE = COMPLETE 2026-09-17**: PR1 ✅ (trusted-bot carve-out, autopilot #491, live); PR2 ✅ via **path (i)** — both env vars live (`DISCORD_SENTINEL_USER_IDS` + `DISCORD_TRUSTED_BOT_IDS`), `author_role` → `sentinel` verified, plus durable deploy-env fix autopilot **#494**; PR3 ✅ UAT (Gary confirmed a real Discord reply). Residual cache bug (`email:null` in `dao_members.json`) tracked non-blocking in `OPEN_FOLLOWUPS.md`, reframed to an ID-native adapter fix. | 2026-09-15 | 923012941937250375 |  |
| `plans/CRF_ANAPU_SUNMINT_COHORT_PROPOSAL.md` | **CRF Anapu × SunMint tree-submission cohort** — `cfr.truesight.me` was built as a static credentialing-cohort mirror (wrong template); this vendors the canonical `sunmint_beta` app into `cfr-anapu` (Option B, Gary 2026-09-15) so tree plantings/monitoring/plots submitted via it are traceable and populate `programs/crf-anapu/members.html`, with itemized per-student click-through. | 2026-09-15 | **blocked on human** — cohort track: PR0–PR5+PR7 complete, RESUME HERE = PR6 (`gate: human`); payout M-track M1–M5 ✅; Q-track (Edgar catalog+sink) Q2/Q3a/Q3b/Q5 ✅ merged+deployed live @32, blocked on governor OAuth re-consent click (no automatable path) | [CFR partnership - figure out how to present](https://t.me/c/3919341801/30026) | 30026 | no | **2026-09-15: Created + committed to main by Claude Anthropic (Envoy)**, at Gary's request, deep-auditing the live stack (sunmint_beta's existing `Submission Source` origin field, the capoeira-shaped `lineage-credentials`/`build_cv_cache.py` cohort pipeline, Butterfly Effect's 83 real members confirmed live) before proposing. **Same-day clarification + approval:** Gary confirmed (a) submission UX stays byte-identical to `sunmint.truesight.me` today (**later re-scoped to the vendored copy (Option B) the same day** after the thin-`?program=`-redirect proved fragile (param dropped across in-app nav)), (b) submission alone (no DAO credential) earns a `members.html` listing, (c) click-through must show itemized trees/monitoring/plots per student (reuses the existing `cv.programs[].recent_events[]` CV mechanism — no new page type), and (d) CEPOTX/Jedielcio already signed off on the privacy posture (aggregate counts on the cohort card, itemized rows linking to each tree's already-public Impact Map/QR entry on click-through, never duplicating GPS/photo on the credential page). **§5a ONE PR PER TURN:** PR0 (pre-flight) ✅ → PR0b (architecture → Option B) ✅ → PR1 (`sunmint_beta` vendor-readiness: un-hardcode `limites-da-fazenda/` `Submission Source`, audit self-refs) ✅ **#84 sha 3732e2a4** → PR2 (vendor the app into `cfr-anapu` `gh-pages`) ✅ **#5 sha f0ffdbda** — live + smoke-checked → PR3 (`lineage-engine` sync job → `lineage-credentials`) ✅ **#23 sha 81e61956** → PR4 (`build_cv_cache.py` activity kind + `program_modes`) ✅ **#24 sha 07486ae0** → PR5 (`program-shell.js` badges + itemized click-through) → PR6 **`gate: human`** (first live sync write) → PR7 (docs + UAT). No prod without UAT gate. **2026-09-16: PR0/PR0b/PR1 verified merged; RESUME HERE = PR2.** **2026-09-17: PR2 verified merged (`cfr-anapu` #5, sha f0ffdbda) + deployed to `cfr.truesight.me` (5-path smoke-check green: `/` = vendored app, `/program/` = credentialing page); RESUME HERE = PR3.** **2026-09-17: PR3 shipped (`lineage-engine` #23, sha `81e61956`) — SunMint program-activity sync job (Option-B attribution, new `sunmint/` kind, dry-run default; 19 tests; live dry-run → 0 attributable today, nothing written). PR4 shipped (`lineage-engine` #24, sha `07486ae0`) — `build_cv_cache.py` now folds the `sunmint/` kind (trees/monitoring/plots counts + itemized `sunmint_events[]`) + tolerant `_program_modes` + a SunMint-inclusive `program_activity_score`; `render_markdown` guards the 0-practice line; 6 new tests, suite 25 passed. RESUME HERE = PR5.** **2026-09-18 (M-track, plan §12.9): payout-entry module M1 ✅ / M2 ✅ (`dapp_beta` #101, live on beta) / M4 ✅ (`dapp_beta` #102, live-beta smoke 5/5) — M4 caught a real duplicate-`id="status"` bug in M2; **M5 ✅ 2026-09-18 (thread 30026): `sync_beta_to_prod(dapp_prod)` merged clean, prod head `89ac500`, live-prod smoke 5/5 on `dapp.truesight.me/report_payout_event.html`** (fork-level promotion carried 4 commits #99/#100/#101/#102; CNAME divergence intact; `withdraw_voting_rights.html` only the permitted `menu.js?v` bump). M0 (Paulo backfill) parked at Gary's request (thread 30026). PR-track reconciled: PR0–PR5 + PR7 complete → RESUME HERE = PR6 (`gate: human`).** **2026-09-18 (later, SS12.7 Q-track): PII Event Envelope un-forked — Gary scoped encryption to the public-JSON-publication layer only (not the browser emitter or the now-private Telegram Chat Logs sheet), so `cfr-anapu#11` (P4) stood as merged+live; SS11.10 + `plans/PII_EVENT_ENVELOPE.md` record it (`agentic_ai_context#1259`). Q2 (`dao_protocol#169`) + Q3a (`dao_protocol#170`) merged; Q5 tabs provisioned on Gary's `cfr program` sheet (`tokenomics#500`/`#502`/`#503`, idempotency bug found+fixed); Q3b sink shipped (`tokenomics#504`, dual-write Tier-1/Tier-2 + retry-dedup) and **deployed live** to the GAS webapp (`1MnAsIQA…`, version progression @30→@32) — independently verified: `payouts` tab materialized on the live Ops workbook with the exact 15 Tier-1 headers. Two real production bugs caught by her own instrumentation and fixed before Paulo's first real submission: (1) the hourly-cron trigger installer was silently swallowed by a try/catch (`tokenomics#513`/`#517`); (2) the installer was categorically unauthorized — `appsscript.json` declared no `script.scriptapp` OAuth scope, so `ScriptApp.getProjectTriggers()` 403's even with a valid clasp token (`tokenomics#518`, deployed @32). **RESUME HERE = blocked on a governor-only step**: the OAuth re-consent click in the Apps Script editor (`clasp run`/Execution API confirmed unusable — no GCP project linked) — no automatable path exists. §12.6 answered by Gary: receipt source = manual/PDF (structured feed deferred), currency = BRL-only, receipt artifact = private governor-only Drive + `receipt_url`. §12.8 placement (`report_payout_event.html` in `dapp_beta`, governor module) shipped+live M2/M4/M5 above. M0 (Paulo backfill) still parked pending the consent click.** | 2026-09-18 |  |  |
| `plans/SPRINT_TRUESIGHT_ME_BOARD_PROPOSAL.md` | **`sprint.truesight.me` — Kanban board over Sophia's handoff pipeline** — a public, fully read-only board so Gary can see at a glance what actually needs his attention across Telegram/Discord, without opening each thread. No auth, no write path — Kanban columns are a direct rendering of `SUPERVISOR_LOOP.md` §2's state enum, defaulting to a "Needs You" view; final step repoints `truesight.me`'s dead Trello link to the new board. | 2026-09-15 | **in progress — live at sprint.truesight.me (HTTP 200, HTTPS cert auto-provisioning); PR0–PR4 + logo/styling shipped; next: PR5 UAT** | [Exec: sprint.truesight.me Kanban board](https://t.me/c/3919341801/30083) | 30083 | no | **2026-09-15: Created + committed to main by Claude Anthropic (Envoy)**, converged over a multi-turn design conversation with Gary this session (external tool → own-subdomain Kanban → SunMint-style auth → advisory-only prioritization → spec+conversation click-through → state-enum columns → default "needs you" view → repoint the dead Trello link). **Same-day simplification:** Gary dropped the signed-priority-vote mechanism entirely ("perhaps I just mention this to envoy instead of a new event") and resolved `quests/join/index.html` to the same treatment as `quests/index.html` ("not invite concept") — the board is now fully read-only/public with **no auth of any kind**; prioritization happens by clicking a card's Telegram/Discord deep link and saying so directly, still advisory only. Deep-audited the real stack before proposing: confirmed `agentic_ai_context` is a **public** repo (`index.json` fetchable with zero new credentials) and located the dead Trello redirects (`truesight_me_prod/quests/index.html` + `quests/join/index.html`, live meta-refresh stubs to a Trello board/invite). **§5a ONE PR PER TURN:** PR0 (pre-flight) ✅ → PR1 (`handoffs/index.json`) ✅ (#1143/#1145) → PR2 (`TrueSightDAO/sprint-site` scaffold) ✅ (#1) → PR3 (DNS/Pages live) ✅ — Envoy independently verified `sprint.truesight.me` returns HTTP 200 with correct DNS to GitHub Pages (2026-09-15; HTTPS cert was still auto-provisioning at that check, expected/normal) → PR4 (advisory-only boundary canonicalized into `SUPERVISOR_LOOP.md`) ✅ (#1158) → **logo/styling PR** ✅ (`sprint-site` #3, Sophia-authored per Gary's direct request for the DAO logo + dapp.truesight.me palette match — Envoy had wrongly self-authored this first as sprint-site #2, closed it, re-delegated to Sophia) → PR5 (UAT) → PR6 **`gate: human`** (repoint `quests/{index,join/index}.html` from Trello — prod-facing). No open decisions remain. RESUME HERE = **PR5 (UAT)**. | 2026-09-15 |  |  |
| `plans/SOPHIA_AUTO_ADVANCE_PR_LESS_UNITS_PLAN.md` | **Stop gating auto-advance on PR-less units** — `app/auto_advance.py::next_action()` treats "no PR opened this turn" as non-convergence, but a correctly-completed unit that isn't PR-shaped (DNS verification, pre-flight/docs-only) can never satisfy that — forcing an unnecessary human "go" every time. Also fixes a confirmed-live root cause of a related halt: a stale duplicate `RESUME HERE` marker. `truesight_autopilot` own-repo — human-merge only. | 2026-09-15 | **completed — PR0–PR5 done 2026-09-15; PR5 docs merged (agentic_ai_context#1209); UAT U1–U5 green; deploy live `1254da2`** | [Exec: auto-advance PR-less units fix](https://t.me/c/3919341801/30279) | 30279 | no | **2026-09-15: Created + committed to main by Claude Anthropic (Envoy)**, at Gary's request, watching the sprint-board handoff (thread 30083) execute live: *"Sophia does auto advance conditionally... if her thread is in the handoff JSON she should just auto advance by default until a gated stage... the interim wait... is too long."* Reproduced twice live in that same thread while writing this up. §5d pre-flight captures the exact code (`app/auto_advance.py::next_action`/`app/main.py::_compute_advance_signal`'s `opened_pr` = literally calling `open_fix_pr`/`open_pr`/`merge_pr`, nothing else counts as progress) and the confirmed root cause of the second incident (Sophia herself diagnosed it: two `RESUME HERE` occurrences by design, "last wins," one edited and one left stale). **§5a ONE PR PER TURN:** PR0 (pre-flight) merged → PR1 (split `opened_pr` into `pr_opened`/`made_progress`, gate only when neither) merged as truesight_autopilot#473 → PR2 (marker-drift detection: connector-required regex + narrow authoritative-unresolvable drift guard, corpus-verified against all 60 plan docs, 0 regressions) reviewed personally by Envoy and merged as truesight_autopilot#474 (`1254da2`) 2026-09-15 20:29 UTC → PR3 **`gate: human`** (deploy — own-repo, human-merge, live-process restart) — correctly self-stopped here, escalated to Gary, never cleared by Envoy → PR4 (UAT, waits on the deploy) → PR5 (docs). One open decision (§6, non-blocking): whether to drop the duplicate-marker convention entirely, deferred until PR2's drift-detection has run for a while. A related, differently-rooted finding (`merge_pr` misreading a path-filtered PR's "0 checks" as pending) filed separately in `OPEN_FOLLOWUPS.md`, not in this plan's scope. RESUME HERE = **COMPLETE** (PR0–PR5 all merged). | 2026-09-15 |
| `plans/SOPHIA_REPO_ACCESS_DENYLIST_PLAN.md` | **Sophia Repo-Access Denylist** — retire the master `allowed_repos` allowlist in favour of default-allow + two protected classes (`contents_api_only_repos`, `protected_repos`) + pattern-gated `create_repo`, so new subdomains/repos stop needing a code change. Triggered by the recurring subdomain-creation friction. | 2026-09-11 | ✅ **COMPLETED 2026-09-15 — closed** | — | — | no | **2026-09-11: Created by Sophia** at Gary's request (thread 25181). Repo **truesight_autopilot** (own-repo gate — opens PRs only, governor merges). PR1–PR6 merged (#1033, #432, #434, #435, #436, #1086); deployed & verified live (default-allow model + create_repo pattern gate + audit hook + de-drifted prompt text active); contribution report filed (480 min). **2026-09-15:** manifest-terminal-marker PR #1127 hit a merge conflict from main churn, superseded by #1134 (merged); close+delete executed — thread 26410 named explicitly in the closing message, then deleted. **PLAN COMPLETE, THREAD CLOSED — nothing further.** | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#orlantildes-coopercabruca` | **Orlantildes / Coopercabruca — 5 kg cacao-butter receipt decision** — decide whether to record the 5 kg Coopercabruca cacao butter Orlantildes delivered (Kirsten's request) as an `[INVENTORY MOVEMENT]` in the ledger (Manager=Orlantildes / Recipient=Matheus Reis), or leave it tracked off-ledger. | 2026-09-14 | blocked — awaiting governor decision on whether to record the 5 kg Coopercabruca cacao-butter receipt in the ledger | [Task tracking](https://t.me/c/3919341801/11042) | 11042 | no | **Found 2026-09-15 by the handoff-index reconciliation sweep** (live thread, no manifest row). The durable follow-up loop fired 2026-09-14 (29.7 days elapsed vs a 14-day threshold): the Orlantildes/Coopercabruca cacao-butter receipt (5 kg, INV-2026-0611-001) is conceptually an inventory receipt from Orlantildes to Matheus's warehouse, tentatively tracked off-ledger. MAPA leg resolved 2026-09-02; GACC still pending. **RESUME HERE = Gary decides record-vs-skip**; if record: `[INVENTORY MOVEMENT]` Manager=Orlantildes / Recipient=Matheus Reis / Inventory Item=Coopercabruca Cacao Butter / Quantity=5 KG. | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#sophia-email-inbox-watch` | **Exec: Sophia email inbox watch** — hourly Gmail inbox-watch loop; governor approved an observation (dry-run) window before enabling outbound sends. | 2026-09-14 | in progress — email-watch observation window enabled on the box (EMAIL_WATCH_ENABLED=true, outbound sends stubbed); the gated process restart is awaiting governor go | [Exec: Sophia email inbox watch](https://t.me/c/3919341801/24143) | 24143 | no | **Found 2026-09-15 by the handoff-index reconciliation sweep** (live thread, no manifest row). Per Gary's go, the observation window was enabled on `/opt/truesight_autopilot` (`.env`: EMAIL_WATCH_ENABLED=true, EMAIL_WATCH_ENABLE_SENDS intentionally unset → all outbound writes stubbed; backup `.env.bak.20260914_031237`). The one unmet step is the **process reload** — a safe autopilot restart Sophia cannot force by hand (guard-blocked); `.env` edits only take effect on restart. **RESUME HERE = governor-authorized safe restart**, then confirm the hourly loop fires and review dry-run output before enabling sends. | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#claude-telegram-monitor-box` | **Exec: capture topic close/reopen signals** — codify multi-chat / discovery support + Telegram topic-lifecycle signal capture on the `nelanco-claude` box's `claude-telegram-monitor`. | 2026-09-15 | in progress — PR #4 merged + service live; 2 housekeeping items still open (PRs #1/#2 await close-out) | [Exec: capture topic close/reopen signals](https://t.me/c/3919341801/30062) | 30062 | no | **Found 2026-09-15 by the handoff-index reconciliation sweep** (live thread, no manifest row). Verified independently on the `nelanco-claude` box: PR #4 merged (`957bb63`, 10:44:29Z) — multi-chat/discovery support + 6 topic-lifecycle signal keys (`FORUM_TOPIC_KEYS`) in `listener.py`; `claude-telegram-monitor.service` active (PID 10304, restart=always); `messages.jsonl` 48,609 lines, growing; `discovered_chats.jsonl` intact. **RESUME HERE = close/confirm PRs #1 and #2** (still open, appear superseded by #4). | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#handoff-index-reconciliation` | **Exec: full handoff-index reconciliation** — diff Sophia's live registries (`sessions/_resume_awaiting.json` + `_discord_resume_awaiting.json`) against `handoffs/index.json`; give every genuinely-open untracked thread/channel a manifest row, leave resolved noise row-less. | 2026-09-15 | **completed — sweep closed 2026-09-15**; re-diffed the live registries against the 59-row index and rowed the verified-open untracked threads (PR1 #1157, PR2 #1164, PR3 #1169, plus 29305); resolved/answered threads left row-less | (this thread 30244) | 30244 | no | **This thread (30244).** Fresh pull closed the stale-checkout gap (local was on an unmerged feature branch). GAP = 61 live Telegram threads + 4 Discord channels not covered by `handoffs/index.json` (48 rows); none of the 61 appear in `HANDOFF_MANIFEST.md` at all. **RESUME HERE = continue adding minimal rows for the verified-open subset**, one PR per batch, regenerating `index.json` before each commit. | **CLOSED 2026-09-15.** Sweep complete: headless-audited `sessions/_resume_awaiting.json` (76 live TG threads) + `_discord_resume_awaiting.json` (4 channels) against `handoffs/index.json`. All 34 candidates triaged — 33 concluded/answered/incidental; **one genuine residual found and rowed: 29305** (in-turn context-compaction plan, `RESUME HERE: Unit 0`, unexecuted). Discord: 2 channels already tracked, 1 archived, 1 (`#server-administration`) deliberately row-less per governor ruling. **RESUME HERE = none — sweep complete.** | 2026-09-15 |  |
| `OPEN_FOLLOWUPS.md#warmup-conversion-delivery-bug` | **Warmup Conversion — follow-up delivery reliability bug** — the recurring "Warmup Conversion 30-day readout" follow-up silently re-triggered ~daily for 13 days (since 2026-09-02) without ever delivering: the direct-HTTP Telegram fallback sent unescaped HTML, the delivery 400'd, and `followup_loop.py` marked the follow-up "resolved" anyway despite the ping never landing. Same bug silently ate 3 other follow-ups the same day (chocolate-subscription-phase2, matheus-nota-fiscal-exportacao, podream-tech-followup). | 2026-09-15 | **blocked on human — fix PR open, awaiting governor merge** | [Exec: Warmup Conversion Readout](https://t.me/c/3919341801/9346) | 9346 | no | **Found 2026-09-15 by Envoy's untracked-thread sweep** (no manifest row existed — thread was invisible to `handoffs/index.json` despite being open since 2026-09-02). Root cause matched Sophia's own 2026-09-02 diagnosis exactly. Fix: `truesight_autopilot` **PR #463** — `_post_to_thread_direct` now `html.escape()`s + drops `parse_mode`; `send_message` gets `require_thread=True` (returns `None` unless delivery is confirmed in-thread, so the loop leaves the follow-up OPEN + retries instead of falsely resolving); 5 new regression tests, full suite green. Opened, **not self-merged** — own-repo gate, governor reviews. Sophia also has the Sep-1 readout data ready to post once delivery is proven working, but flagged that the comparison plan `plans/WARMUP_CONVERSION_IMPROVEMENT_PLAN.md` (referenced by the original follow-up spec) does not exist in this repo — asked Gary directly where §7's targets should live before fabricating a comparison. **RESUME HERE = governor merges #463**, then Gary answers the plan-doc-location question, then Sophia posts the readout. Pairs with the sibling deploy-tree-wipe bug below (thread 29384) — same symptom class, two independent root causes, both needed for a full fix. | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#followup-state-deploy-tree-wipe` | **Follow-up state wiped on every deploy** — independently found the *second* root cause of the same "follow-ups keep re-firing" symptom class as thread 9346: the follow-up resolved-state sidecar (`followups/state.json`) lived inside the tracked deploy tree, so every `deploy_autopilot` run (`git reset --hard` + `git clean -fd`) reverted it to the last-committed snapshot, silently erasing any resolutions since the last deploy — those follow-ups then re-strike on the next deploy. | 2026-09-15 | **blocked on human — fix PR open, awaiting governor merge** | [Fix: follow-up loop drops overdue items](https://t.me/c/3919341801/29384) | 29384 | no | **Found 2026-09-15 by Envoy's untracked-thread sweep** (no manifest row existed). Root cause proven empirically in a scratch repo (reproduced the exact `git reset --hard` semantics that wipe the sidecar). Fix: `truesight_autopilot` **PR #460** — moves the state sidecar out of the deploy tree for durable resolution across deploys. Opened, **not self-merged** — own-repo gate, governor reviews. **Complements PR #463 (thread 9346)** — fixing delivery-confirmation alone doesn't help if the resolved marker itself gets wiped on the next deploy; recommend reviewing/merging both together. RESUME HERE = **governor merges #460**. | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#reservations-settlement-double-booking` | **Reservations & deposits — settlement double-booking bug + tab design** — real financial bug found while designing the reservations/deposits tracking tab: `RSV_STATUS_SOLD` is referenced 3× in the settlement path but defined nowhere → `ReferenceError` on settlement, thrown *after* inventory/liability legs are already booked but *before* the dedupe marker is written → every webhook retry/GAS cron re-books the same transaction → unbounded duplicate ledger rows if the settlement path is ever exercised. | 2026-09-15 | **blocked on human — describe-don't-fix (tokenomics), governor actively engaged, PR scope not yet confirmed** | [Design: reservations & deposits on the ladder](https://t.me/c/3919341801/26819) | 26819 | no | **Found 2026-09-15 by Envoy's untracked-thread sweep** (no manifest row existed despite 500+ messages of substantive work). `tokenomics` is describe-don't-fix per standing SOP — Sophia will not open a PR without Gary's explicit go. Gary is live in this thread working through the design directly with Sophia (confirmed no Reservations tracking tab exists today and there should be one; Sophia asking him to confirm 3 PR-scope sub-items: `RSV_STATUS_SOLD` one-liner, `RSV_SHIPMENT_LISTING_SHEET_NAME`, the Q/R processed-marker). Also held: Gary's A/B/C E2E test-mode choice (Sophia's rec: A, synthetic QR + INVALIDATED-after, *after* this fix — the test isn't a formality, it's what would have caught this) and a "proof mandatory" question. **Supervisor stance: stay out, Gary is driving this conversation directly** — do not insert design opinions, only monitor for the outcome (PR scope confirmed → Sophia opens the tokenomics PR on Gary's explicit go). No live money/reservation event writes without Gary's go. | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#agl16-currency-leak` | **AGL16 [Brazilian Reis] currency-code leak in ledger Column E** — `InsertExpenseRecords()` in the AGL16 expense-processor GAS project wrote the raw `[AGL16] Brazilian Reis` prefix verbatim into the target ledger's Column E instead of the clean currency name whenever the ledger was resolved from Column M / the message (the normal path) — the `[ledger]` prefix strip only ran in an `else` fallback branch that path never hits. Explains the governor's repeated hand-cleaning of that column. | 2026-09-15 | **blocked on human — code fix merged, GAS project awaiting deploy GO** | [AGL16 expenses](https://t.me/c/3919341801/23408) | 23408 | no | **Found 2026-09-15 by Envoy's untracked-thread sweep** (no manifest row existed). Fix: `tokenomics` **PR #492** (merged, `cd2648f6`) — hoists the `[ledger]` prefix strip out of the `else` so it runs unconditionally before any Column E write; routing unchanged; 2 new regression tests. **Still live on the deployed GAS project (`19Wag9x…`) until deployed** — a production/money-adjacent deploy, always-stop gate. Gary's existing hand-cleaned rows are already correct, no backfill needed. RESUME HERE = **governor go → `deploy_gas_project(script_id="19Wag9x-sjbLVgIsPh2vj90ZG7Rgq2iGaVOomAeAvtg6CdZKJHLZ9AJrC", push=true, with_hooks=true)`**, then file a throwaway AGL16 expense with an explicit `- Target Ledger: AGL16` line to verify Column E lands clean. | 2026-09-15 |  |  |
| `OPEN_FOLLOWUPS.md#medicilandia-map-media-archive` | **Medicilandia farmers-convention 2024 — finish the MAP media archive** — archive the 2024 site-visit media (186 files: 72 MOV + 114 HEIC) into the Media Archive Pipeline so the Portuguese content becomes retrievable by English search: merge the 186-item v2 manifest, confirm the S3 raw+preview pass, and add the missing per-photo OCR + vision layer. | 2026-09-15 | in progress — v2 manifest built + validated on branch `medicilandia-convention-manifest` (PR never opened — push PAT lacks `createPullRequest`); 72/72 MOVs enriched (54 speech + 18 silent); S3 archive pass caught mid-flight (~149/186 raw); photo content-extraction layer not yet built | [Medicilandia farmers convention 2024 - Media Archive Pipeline](https://t.me/c/3919341801/23734) | 23734 | no | **Found 2026-09-15 by the handoff-index reconciliation sweep** (live thread, no manifest row, no plan file, no OPEN_FOLLOWUPS entry — this row + its OPEN_FOLLOWUPS entry were filed together at the governor's request). RESUME HERE = (1) open+merge the `farm_media_manifests` PR for branch `medicilandia-convention-manifest` (commit `738eda1`, 186 items); (2) confirm S3 raw+previews reached 186/186; (3) build the per-photo `ocr_text_pt`/`ocr_text_en` + `objects[]`/scene-tag layer (photos are currently video-only for OCR/detection). | 2026-09-15 |  |  |
| `handoffs/PROD_SALES_RECONCILIATION_PLAN.md` | **Prod sales reconciliation - 20260710 Sao Jorge bar batch** - the six 81% Dark Chocolate Bar 50g (Fazenda Sao Jorge) units (QR 2024_20260710_12 / _13 / _14 / _37 / _38 / _39, AGL4) shipped under UPS 1Z0C39140337625102: confirm the net sale price per unit and record the sale (MINTED to SOLD). | 2026-09-15 | completed 2026-09-16 - six SALES EVENTs submitted; all 6 QR codes now SOLD; Stripe key vaulted as stripe_live_key | [20260710 Sao Jorge bars - net sale price](https://t.me/c/3919341801/30870) | 30870 | no | **Found 2026-09-15** answering thread 30870 (see handoffs/PROD_SALES_RECONCILIATION_PLAN.md). Matching sale = Linda Ford subscription renewal (Stripe API-verified 2026-09-16 via Perch live key; sub_1ThahUHrrz4344lszxTsvLeR; invoice in_1UEwYMHrrz4344lsRdOATmVB, 2026-09-12, 6x$10 bar + $10.80 ship = $70.80 gross / $2.35 fee / $68.45 net) $70.80 gross / $2.35 fee, net $11.41/bar (order net $68.45/6 bags), gross $11.80/bar); the six codes are still MINTED and UPS 1Z0C39140337625102 is absent from the Stripe sheet, so the mapping is unconfirmed. RESUME HERE = governor confirms the UPS box maps to Linda Ford's Sept charge; then record SALES EVENT(s) at $11.41/bar net (or $11.80/bar gross). | 2026-09-16 |  |  |
| `handoffs/TERMINAL_STATUS_MARKERS_CONSOLIDATION_PLAN.md` | **Terminal-status marker consolidation** — collapse the three disagreeing “is this handoff terminal?” definitions (index `derive_state()`, `telegram_adapter`, `daily_briefing`) into ONE shared `is_terminal_status()` predicate built on `STATE_RULES`, so `COMPLETE`/`completed` both resolve and the two hand-copied `_TERMINAL_STATUS_MARKERS` tuples (both from commit `38571a1` #438) are deleted. Kills the zombie-handoff-context + daily re-surfacing symptoms. | 2026-09-16 | draft | [Exec: single terminal-status predicate](https://t.me/c/3919341801/30954) | 30954 | no | **Created 2026-09-16** by Sophia, spun out of thread 30870 (Linda Ford subscription sales recon — COMPLETE) while closing that ticket. Root cause: registry *data* was consolidated 2026-07-18 (HANDOFF_REGISTRY_CONSOLIDATION_PLAN) but the terminality *logic* was hand-copied across two consumers, and a third tolerant definition lives in the index builder. RESUME HERE = PR1: shared `is_terminal_status()` + tests in `truesight_autopilot`. | 2026-09-16 |  |  |
| `BLACK_KING_NFE_TRANSIT_CROSSWALK.md` | **Black King NF-e ↔ in-transit reconciliation** — join Black King's (CNPJ 50.042.585/0001-80) 15 export nota fiscal against the Main Ledger's `offchain assets in transit` arrival register (no shared key originally; tracking number + DANFE line-item weights + governor rulings used to construct one), so the governor can hand the accountant an accurate cancelled/shipped/never-shipped breakdown and every `shipment_nfe` row carries a real `Parcel Tracking Number` + `NF-e Chave`. | 2026-09-17 | **completed 2026-09-20** — 12/15 NF-e resolved (10 tracking-matched, 2 cancelled/superseded); 3 documented never-shipped (Exporta Fácil AWB check optional) (NF4/NF7/NF15 — Aug-2025 tariff disruption per governor's read, corroborated by 4 independent evidence legs); Coopercabruca-emitter rows correctly excluded from scope (Rev 5) | [Black King Nota Fiscal and Taxes](https://t.me/c/3919341801/31905) | 31905 | no | **2026-09-17: Created by Sophia** at Gary's request (thread 31905), reconciling SEFAZ consulta-emitente NF-e records against the arrival register. Ad-hoc governor-directed data reconciliation, not a §5a PR-sequence roadmap — living document is the crosswalk file itself (Rev 1→5 dated revisions, each a merged `agentic_ai_context` PR: #1258 stub → #1265/#1266/#1270/#1271/#1275/#1276 cardinality + attribution + scope corrections). Main Ledger writes: `shipment_nfe!K` (Parcel Tracking Number, 10/15 filled) + `!L` (PDF links repointed to full-DANFE blobs, `black-king-nota-fiscal-raw`) + `offchain assets in transit!Q` (NF-e Chave, backfilled with governor-corrected values) — all snapshotted before write, one governor-directed revert already exercised successfully (Kirsten↔Val Lapidus attribution). **RESUME HERE = awaiting the 2025 Exporta Fácil AWBs from Matheus** (the only remaining deterministic key for NF4/NF7's tracking, if they turn out to exist) — otherwise the 3-item gap stands as documented. No prod deploy, no money moved by this thread. | 2026-09-18 |  |  |
| `tokenomics/docs/gas_orphan_mirror_dispositions.md` (repo: **TrueSightDAO/tokenomics** — read via `read_repo_file`, NOT in the agentic_ai_context context-sync mirror) | **GAS Projects security + hygiene cleanup** — org-wide Google Apps Script project audit: hardcoded secrets in live `Credentials.js` accessors, unpushable folders (tracked-file collisions), a public-repo-history-exposed Wix token, broken `clasp push --force` semantics (silently deletes remote-only files `.claspignore` doesn't protect), and property-name drift (`TELEGRAM_BOT_TOKEN` vs canonical `TELEGRAM_API_TOKEN`). | 2026-09-17 | **in progress** — secrets scrubbed from all live accessors + comment literals (`tokenomics#515`/`#519`, 0 real-secret hits in current tree); 4-project push batch healed 2 broken `@HEAD` deploys (`tokenomics` PR series ending `#519`); pre-push remote-only-file guard shipped (`tokenomics#520`, merged) + selective pull-first companion open (`tokenomics#521`) | [GAS projects](https://t.me/c/3919341801/31220) | 31220 | no | **Pre-existing thread (created before this session), picked up by Envoy 2026-09-17 at Gary's request.** Real security finding: the Wix access token (`IST.eyJ…`, fingerprint `2aaefd55359b`) survives in `tokenomics`' PUBLIC git history (commit `63f441e`, 2026-06-16) even though redacted from the working tree — filed `agentic_ai_context#1273` (merged) with the governor decision (revoke-in-Wix vs. history force-push rewrite) explicitly NOT taken unilaterally; Sophia's own analysis recommends revoke-only (a force-push on a public multi-contributor default branch is the single most destructive action available on this box, wouldn't even neutralise the exposure since GitHub retains the blob by SHA absent a Support purge, and the 1 fork keeps it regardless) — **RESUME HERE = governor call on revoke vs. rewrite, still outstanding**. Separately live: PR #520 (remote-only-file delete guard, backed by Google's own `projects.updateContent` docs) merged; PR #521 (pull-first companion, engineered around a real hazard in naive `clasp pull` — it blindly overwrites the whole local tree) still open. No history rewrite, no unauthorized force-push, no GAS deploy without governor go, executed this session. | 2026-09-18 |  |  |

## Known issue: `message_thread_id` 2622 reused across 3 rows

The source registry (`sophia/SOPHIA_HANDOFFS.md`, prior to consolidation) cited Telegram
`message_thread_id` **2622** for three different plans: `SOPHIA_FOLLOWUP_MONITOR_PLAN`
(2026-06-11), `RESEND_VERIFICATION_PLAN` (2026-06-08), and
`SANDBOX_THEOBROMA_1_HANDOFF_DEMO` (2026-06-07). A single Telegram topic should not serve
three unrelated handoffs — this is almost certainly a copy-paste error from whichever row
was written last. None of the three is corrected here because there's no way to determine
the true thread_id from the committed docs alone; each is marked `NEEDS VERIFICATION` above.
**Whoever next touches one of these three handoffs should confirm the real thread in
Telegram and fix the cell.**

## Status values

| Status | Meaning |
|--------|---------|
| `in progress` | Sophia is actively working on this handoff |
| `blocked` | Sophia is waiting on a dependency (human action, PR review, etc.) |
| `demo · live` | Handoff is live/demo, no active execution needed |
| `completed` | All PRs merged, contribution reported, handoff closed |
| `stale` | No activity for 30+ days; may need re-assessment |
| `draft` | Plan written but not yet parked GO-ready in a Telegram topic |
| `parked GO-ready` | Sophia is parked in a Telegram topic, plan complete, awaiting governor "go" |
| `deployed` | Deployed to production; may still have unexecuted follow-up steps |
| `superseded` | Already implemented via another path; this handoff is moot |

## How to update

When Sophia (or any runner) starts, resumes, or completes a handoff, she updates the
relevant row's **Status**, **Telegram topic**, **message_thread_id**, **Resume tracker
state**, and **Last manifest update** columns and commits this file to `main`. Optionally
run `python3 scripts/validate_handoff_manifest.py` first to catch schema/consistency issues
before committing.

The **`Discord channel id`** / **`Discord thread id`** columns track a handoff whose
conversation also runs in a Discord channel (`dc:{guild}:{channel}` sessions — see
`AUTOPILOT_CHANNEL_INTEGRATIONS.md` §2 for the venue registry and channel ids). Populate
them for any row whose work is coordinated in Discord; leave blank for Telegram-only rows.

**Active supervision** — *who is driving a thread right now* — is tracked separately in
`handoffs/active_supervision.json`, not in this table. A supervisor adds an entry to its
`claims` list when it starts actively driving a thread and removes it when it stops (a small,
self-mergeable docs-only PR), so a claim never becomes a heavyweight edit against this manifest.
`scripts/build_handoff_index.py` merges each claim into the index's `supervised_by` field, marking
it stale past ~60 minutes (§2.6 of `plans/SPRINT_TRUESIGHT_ME_BOARD_PROPOSAL.md`).
