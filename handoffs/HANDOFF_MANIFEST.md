# Handoff Manifest — active execution handoffs

Machine-readable index of all active execution handoffs. Most rows hand off to **Sophia** (the
autopilot), who updates them when she starts, resumes, or completes one. Rows may also target
another runner — e.g. the **`nelanco-claude`** interactive box — in which case the row says so
explicitly in its title. Whoever executes updates the row.

**Protocol for all LLMs:**
1. Before acting on any handoff, `git pull` agentic_ai_context remote `main` to get the latest plans.
2. Check this manifest first to see what's active.
3. Cross-reference with `SOPHIA_HANDOFFS.md` for Telegram topic context.

---

| Plan file | Handoff title | Handoff date | Status | Resume tracker state | Last manifest update |
|-----------|---------------|--------------|--------|---------------------|---------------------|
| `plans/GETDATA_IO_SSL_RECOVERY_PLAN.md` | **getdata.io SSL recovery** — replace expired Comodo cert with Amazon ACM auto-renewing cert | 2026-07-18 | **COMPLETE** — all 3 units done | **2026-07-18: Unit 1** (ACM cert requested + DNS validated → ISSUED), **Unit 2** (cert attached to krake-ror-1 ALB HTTPS listener → LIVE), **Unit 3** (3 old Comodo CNAMEs deleted from Route53, expired cert removed from ALB). TLS verified: Amazon RSA 2048 M01, valid 2026-07-18 → 2027-01-31, auto-renewing. | 2026-07-18 |
| `plans/WHITE_LABEL_IMPLEMENTATION_PLAN.md` | **White-label corporate gifting — Phase 2** (state repair + visual placement). **⚠️ Runner = the `nelanco-claude` interactive box, NOT Sophia** — this row is an index entry, not a Sophia parking. | 2026-07-15 | **GO-ready — blocked on Gate D** (`NELANCO_CLAUDE_CODE_BOX_PLAN.md`: Gary must `ssh nelanco-claude` → `tmux` → `claude` login → `/remote-control`; only he can). | **2026-07-14/15: D0 + PR0 + PR1 merged by Claude Anthropic.** Repo **TrueSightDAO/agroverse_shop_beta** → agroverse.shop (**beta-first, §3f — Phase 1 violated this by shipping straight to prod**). **The funnel now works end-to-end for the first time**; suite **34/34 green**. Shipped: **PR0** ([#182](https://github.com/TrueSightDAO/agroverse_shop_beta/pull/182)) committed the implementation, which had been living **100% untracked** despite the plan claiming "deployed to production", + rebuilt the mockup from the real product photo. **D0** ([#183](https://github.com/TrueSightDAO/agroverse_shop_beta/pull/183)) — the label spec was **rotated 90°**: the physical label is **2" W × 4" H portrait** (Gary confirmed; measured 267×520px = 1:1.95) while the page asserted 4"×2" landscape in 7 places, so every design collected would have been unusable. Zero artwork existed yet, so no migration. **PR1** ([#184](https://github.com/TrueSightDAO/agroverse_shop_beta/pull/184)) — B1 (registration dead-ended into an empty card: phantom `#wl-auth-loading` write; error element trapped inside the form it reported on) + B2 (post-payment receipt destroyed ~200ms after render by a two-IIFE race → paying customers saw "No designs yet") + B14 (`cart.js` never loaded → TypeError on every page load). **§5a ONE PR PER TURN:** **RESUME HERE = PR2** (B3 shipping quoted for **stale weight** on qty change — a money bug; B4 gallery never sorts because upload uses legacy `sign()` with no `Timestamp` injection so `created_at` is always `""`; B5 shipping failures swallowed by an empty `catch`) → PR3–PR5 (placement, states A–G) → **PR6 `gate: prod deploy + UAT`**. **Verification method (mandatory — this is what found B1/B2/B4):** Playwright drives each state with stubbed Edgar/GitHub, asserting console errors + which `#wl-*` sections are visible. The pre-existing `white-label-uat.spec.ts` passed while the funnel was dead because it never fires the interaction handlers (§10's `base64ToArrayBuffer` postmortem, repeating). Per §9 every unit needs tests that **fail first**. **Use a `git worktree`** — the DeepSeek session has ~20 uncommitted files in Gary's local `agroverse_shop`. (The box's own `/opt/claude_workspace` clone is clean.) **Open for Gary:** Q1 `agroverse-designs` is a **public** repo (confirmed `visibility: public`) holding unreleased corporate branding, discoverable by hashing a customer's email; Q2 500 bars ≈ 76 lb / 1,000 ≈ 151 lb, both over the **USPS 70 lb** single-parcel limit; Q5 no 600×1200 artwork template exists (**blocks PR4**); and 2 canonical-doc corrections logged in `CONTEXT_UPDATES.md` awaiting an explicit §3 ask. | 2026-07-15 |
| `LARGE_SPIKES_CARD_FIX_AND_CHART_LEGIBILITY_PLAN.md` (in agentic_ai_context — synced to Sophia's context mirror) | `/large_spikes` — disappearing company cards fix + breakout-gauge legibility (follow-up hardening of the LARGE_SPIKE_INDEX build) | 2026-07-05 | **SUPERSEDED — already implemented** (PR #1124 + #1125 merged to sentiment_importer `master` by Claude before this plan was written) | **2026-07-05: Created + committed to main by Claude Anthropic.** Repo **TrueSightDAO/sentiment_importer** (Perch/Rails → perch.truesight.me). Root cause of missing cards = **period-keyed S3 cache miss**: selector offers 1080/3600/7200/10800 but refresher only pre-generates card caches for `[7,30,90,180,360,720,1800]`; card list is fetch-only in prod ⇒ `[]` ⇒ no cards (chart survives — DB-backed). **§5a ONE PR PER TURN:** PR1 (wire the already-set-but-dead `@the_company_period`=180 into the card list so every period resolves; `large_spikes.html.erb` only + regression spec) → PR2 (breakout gauge legibility: `fill:false`→`fill:'end'` shaded over-extended band **[Chart.js v2.9.3 — do NOT use v3/v4 fill objects]** + server-rendered "Today X% · Norm Y% · <regime>" headline; `_spike_chart.html.erb` only + spec) → **DEPLOY+UAT gate** (human runs `./deploy.sh --skip-migrate`, U1–U6). No migration, no money, no data mutation — view-only, revertible. **sentiment_importer own-repo — opens PRs only, NEVER self-merges.** §5d pre-flight complete in the plan. RESUME HERE = **PR1**. | 2026-07-05 |
| `sentiment_importer/LARGE_SPIKE_INDEX_EXECUTION_ROADMAP.md` (repo: **TrueSightDAO/sentiment_importer** — read via `read_repo_file`, NOT in the agentic_ai_context context-sync mirror) | Large Spike Index (`/large_spikes`) — bullish/momentum mirror of the sell-off index (surface breakout/leadership names like MU, INTU for continuation trades; the "other half" of `/large_dips`) | 2026-07-02 | parked GO-ready — thread [8297](https://t.me/c/3919341801/8297) | **2026-07-02: Created + roadmap merged to sentiment_importer `master` (PR #1098) by Claude; parked GO-ready in thread 8297.** Ports the stock-dip **column** architecture to spikes. **§5a ONE PR PER TURN:** PR1 (migration `add large_spike` + `DailyTrade.mark_large_spikes` predicate = new N-day high + `is_surge` volume + `close>sma_20>sma_100` trend gate — **`gate: eyeball predicate` with Gary, expect MU/INTU-class leaders not junk**) → PR2 (`LargeSpikeIndexRefresher` breadth ratio → `LARGE_SPIKE_INDEX` + `LargeSpikerIsFirst`/`mark_first_spike?`; wire enqueue after dip refresher at `daily_trades_importer.rb:72` + `price_refresh_completion_tracker.rb:81`) → PR3 (view data helpers: `interesting_spike_symbols_query`, `large_spikes` helper + S3 cache, `sector_distribution_for_large_spikes`) → PR4 (controller `daily_trades#large_spikes` + route + nav repoint) → PR5 (views + partials + request specs, §9) → PR6 (backfill + **`gate: prod deploy + UAT` on beta**). Reuse universe `companies.sell_off_index=true`; `first_spike` column already exists (crypto index-level use, no collision). §5d pre-flight complete in the roadmap. **sentiment_importer own-repo — opens PRs only, never self-merges.** RESUME HERE = **PR1**. | 2026-07-02 |
| `MEMBERS_PAGE_SINGLE_SOURCE_PLAN.md` | Members page reliability + single-source consolidation (stop `index.json` staleness; make it the single source for Governors + Members + Sentinels so members.html reads one file) | 2026-07-01 | parked GO-ready — thread [8185](https://t.me/c/3919341801/8185) | **2026-07-01: Created + committed to main by Claude.** Root cause: members.html reads 2 drifting caches (`index.json` for gov/members via a 6h cron GitHub drops; `dao_members.json` for sentinels, reliably refreshed). **§5a ONE PR PER TURN:** PR1 (lineage-credentials cron 6h→2h) → PR2 (treasury-cache event-driven poke on dao_members push — **gate: needs `LINEAGE_DISPATCH_PAT` secret**) → PR3 (lineage-engine: emit sentinels + `is_sentinel` into index.json) → PR4 (truesight_me_beta members.html single-source + happy-dom test — **gate: beta→prod human promotion**). PR4 depends on PR3 merged + index rebuilt. Cross-repo — opens PRs only, never self-merges. RESUME HERE = **PR1**. | 2026-07-01 |
| `GOVERNOR_SHEET_PERMISSION_SYNC_PLAN.md` | Governor Sheet-Permission Sync (auto-sync Main Ledger editor list from governor roster) | 2026-06-28 | deployed | **2026-06-28: DEPLOYED.** GAS script `GovernorSheetPermissionSync.js` in project `1m8IZPs...`. Rule: ADD eligible (governor/sentinel with email in Contact sheet) not yet editors; REMOVE only editors IN Contact sheet who aren't governor/sentinel; KEEP everyone not in Contact sheet (GCP SAs, externals). Wired via `doGet(?action=sync_governor_editors)` + daily 04:00 UTC cron. NOT YET EXECUTED — first run adds 5 agent emails. RESUME HERE = run `syncGovernorEditorsNow()` from GAS editor to test. | 2026-06-28 |
| `REVIEW_QUEUE_SOP.md` | Review Queue Standard Operating Procedure (automatable rules for LLM/Sophia review) | 2026-06-28 | draft | **2026-06-28: Created.** Three scenarios: A=name matching (dao_members.json → lineage-credentials, exact match only), B=compute TDG from event Amount (not Grok's estimate), C=Edgar should enforce TDG server-side. Used successfully to process ~475 reviews. Future LLMs should follow this SOP for ongoing review. | 2026-06-28 |
| `SOPHIA_DURABLE_JOURNAL_RESUME_PLAN.md` | Sophia Durable Journal + Checkpoint-Resume Loop (let long work resume in bounded chunks across turns; adds journal+resume on top of existing compaction) | 2026-06-23 | draft — awaiting governor review | **DRAFT, not parked.** §5a one PR per turn; own-repo gate (opens PRs only, never self-merges); flag-gated rollout. Pre-flight gate: **build-incremental vs adopt-a-harness**. RESUME HERE = **PR1** (`app/journal.py` primitive). | 2026-06-23 |
| `QR_SELF_SERVE_CURRENCY_PLAN.md` | Self-Serve QR-Ready Currency Definition via Edgar (`[CURRENCY DEFINITION EVENT]` → QR-ready `Currencies` row, so a new SKU goes zero→QR via Edgar only) | 2026-06-23 | parked GO-ready | **§5a ONE PR PER TURN:** on GO run **PR1 ONLY then STOP** (`dao_protocol`: `define_currency` CLI + `dispatch.py` route). Next turns: PR2 (tokenomics GAS handler in 1N6o00) → PR3 (deploy+wire `DAO_PROTOCOL_WEBHOOK_CURRENCY_DEFINITION`, prod GATE) → PR4 (optional auto-define) → PR5 (docs) → UAT (sandbox/labeled-test gate). Cross-repo gate — **opens PRs only, never self-merges**. Parked GO-ready in **thread 7611** ([topic](https://t.me/c/3919341801/7611)). RESUME HERE = **PR1**. Pre-flight (handler home=1N6o00 reuse deployment `AKfycbxn3siu…`; UAT sheet decision) in plan §3. | 2026-06-23 |
| `SOPHIA_CONTRIBUTION_SCORING_PLAN.md` | Score the contribution review backlog (906 rows / ~90.7k TDG) | 2026-06-23 | in progress | **2026-06-28: Bulk of backlog processed** — ~475 items approved via auto-review (SOP at REVIEW_QUEUE_SOP.md), ~15 rejected, ~5 edge-case skips resolved. Remaining: verify GAS write-back completed, confirm TDG transferred to Ledger. RESUME HERE = verify transfer script ran + report results. | 2026-06-28 |
| `SCORING_REVIEW_QUEUE_PLAN.md` | Scoring Review Queue (cache generator, Edgar endpoint, DApp review UI) | 2026-06-18 | completed | **2026-06-28: COMPLETE.** PR-INTEGRATION fixed (URL prefix, auth via dao_members.json). Review queue processed — ~475 approvals submitted via Deep Seek RSA key, ~15 rejected (email verification events, tests), ~5 skipped (unrecognized names later resolved). Sentinels section on members.html now renders (dao_members.json expanded to 406 contributors). SOP at REVIEW_QUEUE_SOP.md. RESUME HERE = none (done). | 2026-06-28 |
| `SOPHIA_DAPP_EVENT_ALIGNMENT_PLAN.md` | Sophia↔DApp Event Alignment (all event types) | 2026-06-18 | parked GO-ready | Parked GO-ready in **thread 6416** ([topic](https://t.me/c/3919341801/6416)). **ONE PR PER TURN (§5a):** on GO run **PR1 ONLY then STOP** (catalog merge UPDATE — `_refresh_events_catalog` updates required_fields + canonical_labels for events already in the dicts, not just adds new). Then PR2 load-timing → PR3 fallback snapshot → PR4 normalizer (flagged) → PR5 stop-dropping-keys (needs PR4) → PR6 intent→event picking (review gate) → PR7 enforce-lookup (needs PR6) → PR8 UAT. `truesight_autopilot` own-repo gate — opens PRs only, NEVER self-merges; RESUME HERE = PR1. Catalog already complete (30 events) — consumption-correctness effort. | 2026-06-18 |
| `CLI_SALES_EVENT_ALIGNMENT_PLAN.md` | CLI Sales Event Audit & Alignment with DApp | 2026-06-17 | in progress | Parked GO-ready in **thread 6311** ([topic](https://t.me/c/3919341801/6311)); on GO runs PR1 (update CLI report_sales module in dao_client) → PR2 (update Edgar docs) → PR3 (re-submit Gergana's sale); **dao_client repo gate** — opens PRs only, NEVER self-merges; RESUME HERE = PR1 | 2026-06-17 |
| `PUBLIC_KEY_LOOKUP_CACHE_PLAN.md` | Public-Key Lookup Cache (content-addressed per-key governor store; retires the 5-min vault cache-lag bug) | 2026-06-16 | blocked | Parked GO-ready in **thread 5712** ([topic](https://t.me/c/3919341801/5712)). **ONE PR PER TURN (§5a):** on GO run **PR1 ONLY then STOP** (GAS generator emits `treasury-cache/public_keys/<sha256>.json`); next turn resumes PR2 (incremental + revocation); then PR3 (`governor_registry.resolve_key`). Do NOT chain PRs in one turn — that choked Sophia 2026-06-16 (round-cap → empty response; see `CONTEXT_UPDATES.md`). **HOLD PR4 (`vault_routes.py`) until in-flight `track_registry` work merges** (collision); `truesight_autopilot` own-repo gate — opens PRs only, NEVER self-merges; RESUME HERE = PR1; UAT U1–U5 on beta = completion gate | 2026-06-16 |
| `SOPHIA_VAULT_CREDENTIAL_MIGRATION_PLAN.md` | Vault Initialization & Credential Migration (migrate /home/ubuntu/ bare credentials into encrypted vault) | 2026-06-14 | blocked | Parked GO-ready in **thread 3981** ([topic](https://t.me/c/3919341801/3981)); on GO runs Units 1-6: init vault, add 7 credentials, update tools, UAT (U1-U7), docs, cleanup; **own-repo gate**; RESUME HERE = Unit 1 | 2026-06-14 |
| `SOPHIA_LIVE_PROGRESS_PLAN.md` | Live Progress Introspection (report what the executing turn is doing when Gary asks mid-turn, instead of a canned queued ack) | 2026-06-11 | blocked | Parked GO-ready in **thread 2799** ([topic](https://t.me/c/3919341801/2799)); on GO runs PR1 (`_live_progress` record + richer ack) → PR2 (progress-query answered immediately, lock-bypassing); **own-repo gate: opens PRs only, NEVER self-merges**; UAT U1–U5 = completion gate | 2026-06-11 |
| `SOPHIA_MULTI_TENANT_GOVERNANCE_PLAN.md` | Sophia Multi-Tenant Governance, Identity & Vault — **PHASE 0 only** (policy layer + tool-layer auth + data-vs-instruction boundary + guest-default) | 2026-06-11 | blocked | Parked GO-ready in **thread 2744** ([topic](https://t.me/c/3919341801/2744)); on GO runs **Phase 0 only** (RESUME HERE = PR0.1 `app/policy.py`), then STOPS for human merge + Phase 0 UAT (P0.1–P0.4) before Phase 1; **own-repo gate: opens PRs only, NEVER self-merges**; do NOT enter any collaborative group until Phase 0 ships | 2026-06-11 |
| `SOPHIA_FOLLOWUP_MONITOR_PLAN.md` | Durable Follow-up Monitor (thread-bound, multi-day; gmail_reply + elapsed_days; on strike spins a Sophia turn in the thread) | 2026-06-11 | in progress | Parked GO-ready in **thread 2622** ([topic](https://t.me/c/3919341801/2622)); Gary gave GO 2026-06-11; on GO runs PR1→PR4 (RESUME HERE = PR1 `app/followups.py`); **own-repo gate: opens PRs only, NEVER self-merges**; UAT U1–U9 is the completion gate | 2026-06-11 |
| `AUTOPILOT_HARDENING_PLAN.md` | Autopilot Hardening (Sophia's own codebase) | 2026-06-10 | blocked | Parked GO-ready in **thread 2317**; on GO runs Phase 1 PR-A→; **opens PRs only, never self-merges own-repo PRs** (human merges — Phase-2 gate dogfooded) | 2026-06-10 |
| `BETA_SANDBOX_ENDPOINT_PLAN.md` | Beta Sandbox Endpoint (beta.edgar.truesight.me) | 2026-06-09 | blocked | Parked GO-ready in **thread 1955**; on GO runs Unit 1→10, STOPS at operator gates (launch approval / prod deploy / Stripe dashboard); Unit 8 = mandatory AWS infra-doc update | 2026-06-09 |
| `CHOCOLATE_SUBSCRIPTION_PLAN.md` | Agroverse Chocolate Subscriptions — Phase 1 | 2026-06-09 | blocked | Parked GO-ready in **thread 1939** (supersedes 1924); on GO runs Phase 1 PR1.1–1.6 then STOPS at the operator test gate for Gary | 2026-06-09 |
| `RESEND_VERIFICATION_PLAN.md` | Resend verification email | 2026-06-08 | in progress | PR1 merged + deployed; PR2 (oracle) next | 2026-06-08 |
| `SANDBOX_THEOBROMA_1_HANDOFF_DEMO.md` | THEOBROMA-1 (cacao brew demo) | 2026-06-07 | demo · live | — | 2026-06-08 |

## Status values

| Status | Meaning |
|--------|---------|
| `in progress` | Sophia is actively working on this handoff |
| `blocked` | Sophia is waiting on a dependency (human action, PR review, etc.) |
| `demo · live` | Handoff is live/demo, no active execution needed |
| `completed` | All PRs merged, contribution reported, handoff closed |
| `stale` | No activity for 30+ days; may need re-assessment |

## How to update

When Sophia starts, resumes, or completes a handoff, she updates the relevant row's
**Status**, **Resume tracker state**, and **Last manifest update** columns and commits
this file to `main`.
