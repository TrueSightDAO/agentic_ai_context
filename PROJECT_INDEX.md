# Project Index — Per-Project Summary for AI Assistants

Quick reference for each project in the workspace. For credential names and usage, see **agentic_ai_api_credentials**.  
**GitHub:** Clone URLs so future AIs can pull repos (`git clone <URL>`). See also **WORKSPACE_CONTEXT.md** §6 (production domains) and §7 (repo locations).

---

## DAO / Agroverse

| Project | Path | Purpose | Stack | Entry / Key files | Credentials ref | GitHub |
|---------|------|---------|--------|-------------------|------------------|--------|
| **dapp** | `dapp/` | TrueSight DAO DApp: signatures, voting rights, scanner, expenses, feedback. **Store Interaction History:** `store_interaction_history.html` — autocomplete + full sheet context for holistic wellness Hit List (HITL before send); calls tokenomics `holistic_hit_list_store_history` GAS (exec URL in that `.gs` header). | Static HTML/JS, GitHub Pages, Node (tests) | `create_signature.html`, `scanner.html`, `store_interaction_history.html`, `report_dao_expenses.html`, `expense-form-utils.js`, `UX_CONVENTIONS.md`, `tests/README.md`, `.github/workflows/ci.yml` | — | [TrueSightDAO/dapp](https://github.com/TrueSightDAO/dapp) |
| **truesight_me** | `truesight_me/` | TrueSight DAO static site (Wix migration): DAO stats, blog, shipments, Sunmint. **truesight.me** deployed from **truesight_me_prod** | HTML/CSS/JS, Google Sheets | `index.html`, `scripts/`, `google_app_scripts/` | WIX_*, google-service-account.json | [TrueSightDAO/truesight_me](https://github.com/TrueSightDAO/truesight_me) · Prod: [truesight_me_prod](https://github.com/TrueSightDAO/truesight_me_prod) |
| **tokenomics** | `tokenomics/` | DAO tokenomics: Apps Scripts, Python scripts, Raydium market making, schema/API. **GAS:** local **`clasp_mirrors/<scriptId>/`** for `clasp push`/`pull`; git keeps **`.clasp.json`** + checklist/manifest only — **mirror `*.js` / `appsscript.json` gitignored** (`clasp pull` after clone). **`google_app_scripts/`** = reference `.gs` layout. **Clasp:** every mirror includes **`Version.gs`** (sales/ledgers: **`tdg_inventory_management/Version.gs`**; QR Generation: **`agroverse_qr_codes/Version.gs`**; default: **`_clasp_default/Version.gs`**; seed via **`scripts/ensure_clasp_version_gs.mjs`**). **Serialized QR batch (sheet + labels):** **`agentic_ai_context/AGROVERSE_QR_CODE_BATCH_GENERATION.md`**. **Note:** LATOKEN on hold. Wix deprecated. **Holistic Hit List HITL API:** `google_app_scripts/holistic_hit_list_store_history/store_interaction_history_api.gs` (URL in header). | GAS, Python, TypeScript | `clasp_mirrors/`, `google_app_scripts/` (reference), `python_scripts/`, `API.md`, `SCHEMA.md` | QR_CODE_REPOSITORY_TOKEN, GDRIVE_KEY, WIX_* (deprecated), GITHUB_* | [TrueSightDAO/tokenomics](https://github.com/TrueSightDAO/tokenomics) |
| **agroverse_shop** | `agroverse_shop/` | Agroverse e‑commerce (Wix migration): cart, Stripe, blog, farm/shipment pages. `agl15/` redirects to AGL15 Google Sheet ledger. **agroverse.shop** deployed from **agroverse_shop_prod**. **Videos from `~/Downloads` → blog/YouTube:** see **`agentic_ai_context/DOWNLOADS_MEDIA_TO_AGROVERSE.md`** (`analyze_incoming_videos.py`, `youtube_batch_incoming.py`, `generate_video_transcript_blog_posts.py`, `youtube_update_video_titles.py`, `youtube_oauth_reauthorize.py`, `grok_transcript_polish.py`). **Blog listing (`blog/index.html`):** Card thumbnails MUST use **`assets/images/blog/listing-640w/{post-slug}.jpg`** (max edge 640px JPEG ~quality 80), generated from **`scripts/sync_blog_listing_thumbnails.py`**. **Social previews:** After video posts or listing changes, run **`scripts/sync_post_open_graph_images.py`** so **`og:image` / `twitter:image`** match the listing card and include dimensions; use **`AGROVERSE_PUBLIC_ORIGIN`** when the deploy host differs (e.g. beta vs prod) so crawlers get HTTP 200 on images. **Content rule:** Prefer each post’s first real in-body `/assets/images/...` image (normalize `../../assets/`); skip logo; ignore generic `og:image` when it is only `partners/cic/cacao-tasting-wheel.jpg`. Text-only posts: curated distinct farm/partner/`assets/images/blog/bahia-photo-library/` shots. Enforce **unique** thumbnail URL and **unique** file hash across the grid; replace duplicates using `bahia-photo-library` or other repo assets. **Cursor rule:** `agroverse_shop/.cursor/rules/blog-listing-images.mdc`. **Lab report translation:** See `LAB_REPORT_TRANSLATION.md` when adding English summaries to shipment pages (AGL4, AGL8, etc.). **Physical product / packaging specs:** Ground truth for copy, colors, QR patterns — see **`PRODUCT_DEVELOPMENT_SPECS.md`** (Sheets workflow; service account `google-service-account.json` for API). **Important:** See `docs/PRODUCT_CREATION_CHECKLIST.md` when creating new products to prevent Merchant Center URL mismatches. **New SKU — farm + shipment `item-card` grids:** **`agentic_ai_context/AGROVERSE_SHOP_NEW_SKU_WEB_CHECKLIST.md`**. **⚠️ SETUP REQUIRED:** See `docs/SECURITY.md` — requires credential files: `google-service-account.json`, `scripts/youtube_credentials.json`, `scripts/youtube_token.json` (NEVER commit these; prompt user during setup). | Static HTML/JS, Google Apps Scripts, Node (tests) | `scripts/`, blog in `post/`, `shipments/`, `agl15/`, `scripts/sync_blog_listing_thumbnails.py`, `scripts/sync_post_open_graph_images.py`, `scripts/translate_lab_report.py`, `tests/README.md`, `playwright.config.ts`, `.github/workflows/visual-consistency.yml`, `docs/PRODUCT_CREATION_CHECKLIST.md`, `docs/SECURITY.md` | NAMECHEAP_*, GOOGLE_API_KEY, AWS_* | [TrueSightDAO/agroverse_shop_beta](https://github.com/TrueSightDAO/agroverse_shop_beta) · Prod: [agroverse_shop_prod](https://github.com/TrueSightDAO/agroverse_shop_prod) |
| **dao_client** | `dao_client/` | Python client + CLI for the DAO contribution server (Edgar). **`edgar_client.py`** is the port of `dapp/scripts/edgar_payload_helper.js` — RSA-2048 key gen (SPKI/PKCS#8 b64, WebCrypto-identical), canonical payload formatting, RSASSA-PKCS1-v1_5 / SHA-256 signing, multipart POST to `/dao/submit_contribution`. **`auth.py`** runs an OAuth-loopback email flow: `login` binds a random `127.0.0.1:<port>/verify` listener, signs `[EMAIL REGISTERED EVENT]` with that URL as generation source, GAS mailer uses it as the email link, clicking captures `em+vk` and signs `[EMAIL VERIFICATION EVENT]` automatically. Fallbacks: `verify --vk <value\|URL>`, `status`, `rotate`. Keys persisted to `.env` (0600, gitignored). **Same-device constraint:** loopback ports don't leave 127.0.0.1; click the email link on the machine that ran `auth.py login`, or fall back to `verify --vk`. **Validated end-to-end 2026-04-21:** first `auth.py login` → email click → Edgar returned `signature_verification: success` + `activated: true`; `auth.py status` shows `registered: true, contributor_name: Gary Teh`. **`modules/`** — 15 per-event CLI wrappers mirroring every signed page at `dapp.truesight.me/` (run `python3 modules/<page>.py --help` for named flags + `--attr 'Label=Value'` escape hatch; `--dry-run` prints the signed share text). **`dapp_digital_signature_onboarding/`** is the Edgar-side sheet-flow demo (previously lived in tokenomics). **AI agent `[CONTRIBUTION EVENT]`:** **`modules/report_ai_agent_contribution.py`** — supply **`https://github.com/TrueSightDAO/.../pull/N`** URLs and an explicit description per **`agentic_ai_context/DAO_CLIENT_AI_AGENT_CONTRIBUTIONS.md`**. **Retail-partner onboarding (MVP, 2026-04-28):** **`modules/onboard_retail_partner.py`** — manifest-driven CLI that automates `[CONTRIBUTOR ADD EVENT]` (with `<First> - <Store>` naming), `Contributors!U` (mailing address; explicitly NOT col T which is online-fulfillment-only), `Agroverse Partners` row append, opening-order `[INVENTORY MOVEMENT]` loop, and inventory + velocity sync runs. Idempotent on every step; `--dry-run` default. Manifest schema and worked example at `dao_client/examples/onboarding/the-way-home-shop.yaml`; full invocation in **`agentic_ai_context/RETAILER_TECHNICAL_ONBOARDING.md` §10** ([dao_client#11](https://github.com/TrueSightDAO/dao_client/pull/11)). **Serialized QR bulk sales:** see `examples/bulk_qr_sales_template.py` and `agentic_ai_context/notes/claude_serialized_qr_sales_*.md` for the `[SALES EVENT]` per-QR-code pattern + `[INVENTORY MOVEMENT]` depletion workflow. | Python | `edgar_client.py`, `auth.py`, `modules/`, `modules/report_ai_agent_contribution.py`, `modules/onboard_retail_partner.py`, `dapp_digital_signature_onboarding/`, `examples/onboarding/` | `.env` (`EMAIL`, `PUBLIC_KEY`, `PRIVATE_KEY` — never commit); `google_credentials.json` (for sheet writes during onboarding — searched in `$DAO_CLIENT_GOOGLE_CREDENTIALS`, `dao_client/`, then `../market_research/`) | [TrueSightDAO/dao_client](https://github.com/TrueSightDAO/dao_client) |
| **qr_codes** | `qr_codes/` | QR code assets / generation (linked to tokenomics/agroverse) | — | — | — | — |
| **proposals** | `proposals/` | DAO proposals (minimal content in workspace) | — | `README.md` | — | — |
| **TrueChain** | `TrueChain/` | **Designed but not running as of 2026-04-22.** Planned private Ethereum network for DAO/Agroverse audit trail + future on-chain commerce + governance. Repo ships genesis + sample contracts + Mirror-Service design, but no validators currently producing blocks and no Mirror Service actively copying Sheets → chain. See **TRUECHAIN.md** for the design and **BLOCKCHAIN_ANCHORING_PROPOSAL.md** §3 for the conditional framing (TrueChain only justifies standing up if TDG becomes on-chain settlement medium OR governance outcomes need external verifiability; otherwise the simpler L2→L4 batched-timestamping path is strictly better). | Geth, Solidity, Truffle | `README.md`, `genesis.json`, `truffle_actual/contracts/` | TRUECHAIN_RPC_URL (Mirror Service — unused today) | [TrueSightDAO/TrueChain](https://github.com/TrueSightDAO/TrueChain) |

---

## Krake / Data Harvesting

| Project | Path | Purpose | Stack | Entry / Key files | Credentials ref | GitHub |
|---------|------|---------|--------|-------------------|------------------|--------|
| **krake_ror** | `krake_ror/` | Krake Rails backend (data harvesting engine). **Production:** **`https://getdata.io`**. **Not** Edgar — **`edgar.truesight.me`** is **`sentiment_importer`** (see **WORKSPACE_CONTEXT.md** §6). | Ruby 2.6, Rails, PostgreSQL, Redis, Elasticsearch | `rails s`, `lib/tasks/` | SENDGRID_API_KEY | [KrakeIO/krake_ror](https://github.com/KrakeIO/krake_ror) |
| **krake_sinatra** | `krake_sinatra/` | Krake Sinatra services | Ruby, Sinatra | — | — | [KrakeIO/krake_sinatra](https://github.com/KrakeIO/krake_sinatra) |
| **krake_local** | `krake_local/` | Local Krake tools (commander, listener, Chrome listener) | Node/TypeScript | `krake_commander/`, `krake_chrome_listener/` | — | — |
| **krake_chrome** | `krake_chrome/` | Krake Chrome extension (data harvesting UI) | JS, CoffeeScript, Jasmine | `manifest.json`, `js/`, `spec_j/` | — | [KrakeIO/Chrome](https://github.com/KrakeIO/Chrome) |

---

## Content & Research

| Project | Path | Purpose | Stack | Entry / Key files | Credentials ref | GitHub |
|---------|------|---------|--------|-------------------|------------------|--------|
| **market_research** | `market_research/` | Content calendars, physical stores, blog/social sync for Agroverse & TrueSight. **Hit List `Research` → Places + Grok:** `scripts/hit_list_research_photo_review.py`; CI: `.github/workflows/hit_list_research_photo_review.yml`. **Hit List `AI: Enrich with contact` → website email / contact form:** `scripts/hit_list_enrich_contact.py`; shared DApp Remarks apply: `scripts/hit_list_dapp_remarks_sheet.py`; CI: `.github/workflows/hit_list_enrich_contact.yml` (hourly :35 UTC). Design + narrative: **`agentic_ai_context/HIT_LIST_CONTACT_ENRICHMENT.md`**. **Lead list extraction:** Playwright (Google Maps/Yelp) → `apothecary_discovery.csv` → `append_to_hit_list.py` → Hit List. See `LEAD_LIST_EXTRACTION.md`. **Product dev specs:** `scripts/populate_chocolate_bar_spec_sheet.py` populates Google Sheet checklists (tabs per section) + cell styling for packaging/SKU work; see **`PRODUCT_DEVELOPMENT_SPECS.md`**. **Purchase agreement PDFs:** `purchase_agreements/` (ReportLab); workflow + farm URLs in **`PURCHASE_AGREEMENT_PDFS.md`**. **Gmail OAuth (user inbox):** `scripts/gmail_oauth_authorize.py`, `credentials/gmail/` (secrets gitignored); see **`GMAIL_OAUTH_WORKFLOW.md`**. **Hit List Email Agent:** tab **Email Agent Suggestions** (draft registry + `ensure_`/`format_` scripts); **Email Agent Follow Up** (sent log); **`PARTNER_OUTREACH_PROTOCOL.md`** (human-in-the-loop outreach). **Agroverse newsletter (community email):** `scripts/send_newsletter.py` drafts or sends via Gmail API from `garyjob@agroverse.shop`, logs each message to Main Ledger tab **`Agroverse News Letter Emails`**, optionally embeds an Edgar tracking pixel (`GET /newsletter/open.gif`); body drafts live in `newsletter_drafts/`; subscribers come from Main Ledger tab **`Agroverse News Letter Subscribers`** (`CONFIRMED` only); label convention `Newsletter/<Campaign>`. See **`AGROVERSE_NEWSLETTER_WORKFLOW.md`** (register, schema, reviewer convention, anti-patterns). **Fresh machine:** **`.env.example`** → **`.env`** (incl. **`GITHUB_PAT`** for **`TrueSightDAO/.github`** uploads; see **`WORKSPACE_CONTEXT.md`** §**3c**). **GitHub (automation source):** [TrueSightDAO/go_to_market](https://github.com/TrueSightDAO/go_to_market) — clone name may differ locally (`content_schedule`, etc.). | Python, venv | `physical_stores/`, `ceremonial_cacao_seo/playwright/`, `purchase_agreements/`, `credentials/gmail/README.md`, `scripts/gmail_oauth_authorize.py`, `append_to_hit_list.py`, `scripts/populate_chocolate_bar_spec_sheet.py`, `scripts/hit_list_enrich_contact.py`, `scripts/hit_list_dapp_remarks_sheet.py`, `HIT_LIST_CREDENTIALS.md`, **`.env.example`** | **`.env`** (local): **`GITHUB_PAT`**, `GOOGLE_CALENDAR_ID`, `DEFAULT_TIMEZONE`, **`GOOGLE_MAPS_API_KEY`**, **`GROK_API_KEY`** (photo + contact enrich), **`google_credentials.json`** | [TrueSightDAO/go_to_market](https://github.com/TrueSightDAO/go_to_market) (see also [content_schedule](https://github.com/TrueSightDAO/content_schedule)) |
| **video_editor** | `video_editor/` | AI video editor: Whisper, YOLO, Grok, YouTube Shorts upload | Python, Flask | `app.py`, `video_queue.py`, `grok_client.py` | GROK_API_KEY, PORT | — |
| **sentiment_importer** (Edgar) | `sentiment_importer/` | **Production:** **`https://edgar.truesight.me`** (Rails “Edgar”). **Not** **`getdata.io`** — that is **krake_ror**. DAO API (submit_contribution, proposals, votes); news/stock import; **Agroverse checkout shipping** `GET` **`/agroverse_shop/shipping_rates`** (EasyPost USPS, same as GAS—**agroverse_shop** `js/config.js` `shippingRatesApiOrigin`); Sidekiq **`AgroverseInventorySnapshotPublishWorker`** (GAS snapshot to GitHub inventory JSON—**`AGROVERSE_INVENTORY_*`** env on host). **Newsletter open tracking:** `GET /newsletter/open.gif?mid=<uuid>&r=<b64 email>` returns 1x1 GIF and updates Main Ledger tab **`Agroverse News Letter Emails`** via `Gdrive::NewsletterEmails` (uses `config/edgar_dapp_listener_key.json`); embedded by `market_research/scripts/send_newsletter.py --track-opens`; see **`AGROVERSE_NEWSLETTER_WORKFLOW.md`**. **Production deploys + runtime gotchas:** see **`NOTES_sentiment_importer.md`** (topology, `./deploy.sh` flow, `SECRET_KEY_BASE`-from-systemd pattern, **no auto-deploy on merge**, migration-safety caveats). | Rails, PostgreSQL, Redis, Elasticsearch, Sidekiq | `rails s`, workers, `dao_controller.rb`, `agroverse_shop_shipping_rates_controller.rb`, **`deploy.sh`** | ALPHA_VANTAGE_*, FMP_*, POLYGON_*, IEX_*, WIX_*, HELLOCASH_*, EASYPOST_*, **AGROVERSE_INVENTORY_*** | — |
| **garyteh_blog** | `garyteh_blog/` | Personal blog (static or generated) | HTML/JS | — | — | — |

---

## Infra & Tools

| Project | Path | Purpose | Stack | Entry / Key files | Credentials ref | GitHub |
|---------|------|---------|--------|-------------------|------------------|--------|
| **jarvis** | `jarvis/` | Local LLM web service (e.g. Qwen), FastAPI | Python, FastAPI, Hugging Face | `app.py`, `start.sh` | SYSTEM_PROMPT, MODEL_NAME, USE_4BIT, etc. (config) | — |
| **read_page** | `read_page/` | Browser extension (read page / content) | JS, manifest | `manifest.json`, `content.js` | — | — |
| **iching_oracle** | `iching_oracle/` | I Ching oracle app | Node/TypeScript | — | — | — |
| **point-of-sales-integrations** | `point-of-sales-integrations/` | POS integrations | — | — | — | — |
| **heierling-pos** | `heierling-pos/` | Heierling POS (small codebase) | HTML/JS | — | — | — |
| **Cypher-Defense** | `Cypher-Defense/` | Web3 scam / impersonation / phishing awareness (extension + blacklist assets). **Also:** AWS incident-response helpers — `scripts/aws/` (e.g. `delete_ec2_keypair_all_regions.sh`, `terminate_ec2_by_launch_keypair.py`), `docs/incidents/` (dated analyses). **Credentials:** repo-root `.env` (**gitignored**); see `.env.example`. | JS, Handlebars, AWS CLI, Python | `README.md`, `scripts/aws/`, `docs/incidents/`, `blacklist/` | **`Cypher-Defense/.env`** (`AWS_KEY` / `AWS_SECRET` or standard `AWS_ACCESS_KEY_ID` / `AWS_SECRET_ACCESS_KEY`) | — |
| **truesight_autopilot** | `truesight_autopilot/` | **Unified AI service for TrueSight DAO — governor chat + autonomous SRE + developer.** Merged from `governor_chatbot_service` (now deprecated). Two modes: (1) **Reactive** — governor chat via DApp `chat.html` (`POST /chat`, RSA-signed or JWT, DeepSeek-V3 with tool_calls for reading context/repos and opening fix PRs); (2) **Proactive** — background Gmail polling (GH Action failures, GAS errors), AWS CloudWatch/Cost Explorer monitoring, autonomous PR opening. Diagnoses with DeepSeek-V3 (~30× cheaper than Claude/Kimi). Dropped Kimi + Claude. Logs every fix as `[CONTRIBUTION EVENT]` to Edgar via dedicated identity. Never auto-merges. Deploys to **dedicated EC2** separate from `seni_ror` (Edgar) to protect critical infrastructure. See `README.md` for vision, architecture, and safety principles. | Python, FastAPI, DeepSeek, PyGithub | `app/main.py`, `app/chat.py` (in main), `app/auth.py`, `app/context.py`, `app/email_poller.py`, `app/llm_client.py`, `app/github_client.py`, `app/aws_monitor.py`, `scripts/deploy.sh`, `systemd/truesight-autopilot.service` | `TRUESIGHT_DAO_AUTOPILOT`, `GMAIL_TOKEN_JSON`, `DEEPSEEK_API_KEY`, `JWT_SECRET`, dedicated Edgar keypair (`EMAIL`/`PUBLIC_KEY`/`PRIVATE_KEY`), AWS creds or IAM role | [TrueSightDAO/truesight_autopilot](https://github.com/TrueSightDAO/truesight_autopilot) |
| ~~**governor_chatbot_service**~~ | ~~`governor_chatbot_service/`~~ | ~~**DEPRECATED — merged into `truesight_autopilot`.** Conversational AI for DAO governors.~~ | ~~Python, FastAPI, Kimi~~ | ~~`app/main.py`~~ | ~~`KIMI_API_KEY`~~ | ~~[TrueSightDAO/governor_chatbot_service](https://github.com/TrueSightDAO/governor_chatbot_service)~~ |

---

## Compliance / Biz

| Project | Path | Purpose | Stack | Entry / Key files | Credentials ref | GitHub |
|---------|------|---------|--------|-------------------|------------------|--------|
| **fda_fsvp** | `fda_fsvp/` | FDA FSVP docs, supplier verification, declarations | PDFs, docs | `public_declarations/`, `suppliers/`, `regulations reference documents/` | — | — |

---

## Context & Credentials (no code)

| Project | Path | Purpose | GitHub |
|---------|------|---------|--------|
| **agentic_ai_context** | `agentic_ai_context/` | This repo: workspace context for AIs (`WORKSPACE_CONTEXT.md`, `PROJECT_INDEX.md`, `SUPPLY_CHAIN_AND_FREIGHTING.md` for supply chain/freighting/unit-cost). **OpenClaw + WhatsApp** (JIDs, monitor intent, exclusions, playbook): **`OPENCLAW_WHATSAPP.md`**; **`WORKSPACE_CONTEXT.md` §3d** points there. **Beer Hall vs Founder Haus AI digests:** same file — **§ Outbound digests**, **Beer Hall — two WhatsApp messages** (**Message 1** attribution + **TLDR** only; **Message 2** *Shipped* links + optional Telegram block), **§ Gathering what merged** (poll all relevant **`TrueSightDAO`** repos: **`git fetch`** / **`git log`** / **`gh pr list`** for the target day), **§ Gathering Telegram Chat Logs** ([sheet tab](https://docs.google.com/spreadsheets/d/1qbZZhf-_7xzmDTriaJVWj6OZshyQsFkdsAV8-pyzASQ/edit#gid=0); helper **`market_research/scripts/list_recent_telegram_chat_logs_for_digest.py`**); **preview / review recent progress** (**`market_research/scripts/generate_beer_hall_preview.py`** → **`agentic_ai_context/previews/beer_hall_preview_latest.md`** + **stdout** (see **`OPENCLAW_WHATSAPP.md` § Preview digest**)), and **§ Closed loop** (Founder Haus: **single** shorter **AI-only** message by default; both require **OpenClaw × Cursor** attribution, WhatsApp-safe formatting, **TLDR** with explicit **DAO relevance** for non-technical readers, and **GitHub links limited to `github.com/TrueSightDAO/`** — not personal or KrakeIO repos). **After every WhatsApp send:** run **`market_research/scripts/append_openclaw_beer_hall_log.py`** so tab **OpenClaw Beer Hall updates** on the Telegram compilation sheet is updated in the **same session** (OpenClaw does not write the sheet by itself). **`DOWNLOADS_MEDIA_TO_AGROVERSE.md`** — heuristics when the user drops **videos** or **images** in **`~/Downloads`** for **agroverse.shop** (analyze → YouTube → blog + Grok polish; images → copy into `assets/` + `sync_blog_listing_thumbnails.py`). **GitHub SSH for agents:** `GITHUB_AGENTIC_AI_SSH.md` — key dir **`~/.ssh/agentic_ai_github/`** (not in git); **push convention:** always **`feature/<topic>`** / **`fix/<topic>`** branched from the repo default — **not** ambiguous local branch names left over from other work (see doc § Pull requests); **merge to default branch** only when the user explicitly asks (`gh pr merge` per that doc). **Main Ledger repackaging:** `LEDGER_CONVERSION_AND_REPACKAGING.md` (canonical); `WORKSPACE_CONTEXT.md` §3b is the mandatory pointer. **Retail partner playbook (interim):** `PARTNER_OUTREACH_PROTOCOL.md` — consignment “yes” stages, evidence links, human-in-the-loop follow-up workflow (phased). | [TrueSightDAO/agentic_ai_context](https://github.com/TrueSightDAO/agentic_ai_context) |
| **agentic_ai_api_credentials** | `agentic_ai_api_credentials/` | Credential **reference** only: `env.template`, `API_CREDENTIALS_DOCUMENTATION.md`. No secrets. | — |

---

*When adding or removing projects, update both WORKSPACE_CONTEXT.md and this PROJECT_INDEX.md.*
