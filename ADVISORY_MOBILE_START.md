# Advisory — quick start (phone / Claude)

Read in this order:

1. **Stable DAO orientation** (ledgers, repos, invariants, strategy prompts):  
   https://raw.githubusercontent.com/TrueSightDAO/ecosystem_change_logs/main/advisory/BASE.md

2. **Machine index** (`read_order`, `canonical_context_urls`, snapshot list):  
   https://raw.githubusercontent.com/TrueSightDAO/ecosystem_change_logs/main/advisory/index.json

3. **Latest evidence digest** (git window + `CONTEXT_UPDATES` + Beer Hall excerpts):  
   See newest `advisory/snapshots/YYYY-MM-DD.md` under the same repo — pick the date from `index.json` → `snapshots[0].markdown`, then open:  
   `https://raw.githubusercontent.com/TrueSightDAO/ecosystem_change_logs/main/<path from index>`

Local working copy (Cursor): `ADVISORY_SNAPSHOT.md` in this repo (regenerated by `market_research/scripts/generate_advisory_snapshot.py`).
